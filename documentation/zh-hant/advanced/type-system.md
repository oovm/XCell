# 類型系統（Type System）

XCell 支援多种資料類型，标准類型采用 Rust 約定，同时相容 C 約定和 C++ 約定。類型不區分大小寫。

## 基本類型

### 整數類型

| 标准寫法  | Unity 約定             | Unreal 約定  | 描述       | 范圍                                         |
| ----- | -------------------- | ---------- | -------- | ------------------------------------------ |
| `i8`  | `char`               | `int8_t`   | 8位有符號整數  | -128 到 127                                 |
| `u8`  | `unsigned char`      | `uint8_t`  | 8位无符號整數  | 0 到 255                                    |
| `i16` | `short`              | `int16_t`  | 16位有符號整數 | -32768 到 32767                             |
| `u16` | `unsigned short`     | `uint16_t` | 16位无符號整數 | 0 到 65535                                  |
| `i32` | `int`                | `int32_t`  | 32位有符號整數 | -2147483648 到 2147483647                   |
| `u32` | `unsigned int`       | `uint32_t` | 32位无符號整數 | 0 到 4294967295                             |
| `i64` | `long long`          | `int64_t`  | 64位有符號整數 | -9223372036854775808 到 9223372036854775807 |
| `u64` | `unsigned long long` | `uint64_t` | 64位无符號整數 | 0 到 18446744073709551615                   |

<br />

### 浮點數類型

| 标准寫法  | C 約定     | C++ 約定   | 描述     | 精度  |
| ----- | -------- | -------- | ------ | --- |
| `f32` | `float`  | `float`  | 32位浮點數 | 單精度 |
| `f64` | `double` | `double` | 64位浮點數 | 雙精度 |

### 布爾類型

| 标准寫法   | C 約定   | C++ 約定 | 描述  | 值                |
| ------ | ------ | ------ | --- | ---------------- |
| `bool` | `bool` | `bool` | 布爾值 | `true` 或 `false` |

### 字元類型

| 标准寫法    | 描述          |
| ------- | ----------- |
| `utf8`  | UTF-8 字元類型  |
| `utf16` | UTF-16 字元類型 |

### 字串類型

| 标准寫法     | 描述                 |
| -------- | ------------------ |
| `string` | 字串類型              |
| `str`    | 字串類型（與 string 等效） |

## 復合類型

### 陣列與向量類型

**不同類型的約定**：

| 标准寫法        | C# 約定            | 描述         |
| ----------- | ---------------- | ---------- |
| `[T]`       | `T[]`            | 動態陣列       |
| `[T; N]`    | `T[N]`           | 静態陣列（固定大小） |
| `Vec<T>`    | `List<T>`        | 向量/串列類型    |
| `vec2<f32>` | `Vector2<float>` | 2D向量       |
| `vec3<f32>` | `Vector3<float>` | 3D向量       |
| `vec4<f32>` | `Vector4<float>` | 4D向量       |

**說明**：

- 動態陣列 `[T]` 和向量 `Vec<T>` 用於儲存可變數量的元素
- 静態陣列 `[T; N]` 用於儲存固定數量的元素，长度在定義时確定
- 特殊向量類型（vec2/vec3/vec4）在圖像程式設計中常用，Unity 等引擎會有特定的效能最佳化

**範例**：

- `[i32; 5]` - 包含5個i32元素的静態陣列
- `[string]` - 動態大小的字串陣列
- `Vec<i32>` - 整數向量
- `vec3<f32>` - 3D浮点向量

### 字典類型

**格式**：`HashMap<K, V>` 或 `dict<K, V>`

**範例**：

- `HashMap<string, i32>` - 字串到整數的對應
- `dict<i32, string>` - 整數到字串的對應

### 元組類型

**格式**：`(T1, T2, ...)`

**範例**：

- `(i32, string)` - 包含整數和字串的元組
- `(f32, f32, bool)` - 包含兩個浮點數和一個布爾值的元組

## 特殊類型

### 顏色類型

| 類型      | 描述               | 格式                  |
| ------- | ---------------- | ------------------- |
| `color` | 顏色類型             | 十六進位顏色值，如 `#FF0000` |
| `Color` | 顏色類型（與 color 等效） | 十六進位顏色值，如 `#FF0000` |

### 时间類型

| 類型         | 描述     | 格式                                  |
| ---------- | ------ | ----------------------------------- |
| `datetime` | 日期时间類型 | ISO 8601 格式，如 `2023-12-25T10:30:00` |
| `time`     | 时间類型   | 如 `10:30:00`                        |
| `date`     | 日期類型   | 如 `2023-12-25`                      |

<br />

## 唯一類型與主鍵

**唯一類型格式**：

- `@T` - 資料表示该欄位的值在資料表中必须唯一，即資料表中不能有兩行資料在该欄位上有相同的值
- `@@T` - 資料表示该欄位的值在資料表中必须唯一且作為主鍵

**主鍵說明**：

- 主鍵是資料表中用於唯一標識每一行資料的欄位
- 如果資料表中没有使用 `@@T` 显式標記主鍵，那麼資料表的第一列會自動被视為主鍵
- 主鍵值必须唯一且非空，用於在資料表之间建立關联關系

**範例**：

- `@@i32` - 整數類型的主鍵欄位
- `@string` - 字串類型的唯一欄位（非主鍵）

## 自訂類型

### 列舉類型

通过 Enumerate 資料表格定義的列舉類型可以在其他資料表格中使用。

**範例**：

- `QualityType` - 參照列舉資料表中定義的品質類型

### 結構體類型

通过 Class 資料表格定義的結構體類型可以在其他資料表格中使用。

**範例**：

- `Position` - 參照 Class 資料表格中定義的位置結構體

## 類型转换

XCell 支援自動類型转换，例如：

- 整數可以自動转换為浮點數
- 浮點數在需要时可以转换為整數（會截断小数部分）
- 数字可以转换為字串
- 字串可以在适當情況下转换為数字

## 類型驗證

XCell 在剖析資料表格时會進行類型驗證，確保資料符合指定的類型要求。如果類型不匹配，會在編譯时生成錯誤。
