import { writeFileSync, mkdirSync } from 'fs';
import { join, dirname } from 'path';

const base = 'e:/灵之镜有限公司/XCell/documentation/ru';

function write(filePath, content) {
  const fullPath = join(base, filePath);
  mkdirSync(dirname(fullPath), { recursive: true });
  writeFileSync(fullPath, content, 'utf-8');
  console.log(`Created: ${filePath}`);
}

write('readme.md', `# XCell

XCell — это инструмент управления конфигурационными таблицами, написанный на Rust, предназначенный для игрового движка Unity.

## Быстрый старт

### Установка

Скачайте последнюю версию \`xcell.exe\` и поместите её в корневую директорию проекта.

### Настройка проекта

Создайте файл \`XCell.toml\` в корневой директории проекта:

\`\`\`toml
version = "0.0.0"

exclude = ""
include = "*.xlsx"

line.field = 1
line.type = 2
line.comment = 3
line.data = 4

[type.bool]
accept = ["true", "√"]
reject = ["false", "x"]

[type.string]

[unity]
enable = true
project = "./"
output = "Assets/Scripts/DataTable/Generated"
namespace = "DataTable.Generated"
manager = "DataTableManager"
suffix_table = "Table"
suffix_element = "Element"
support_clone = true
legacy_using = false
legacy_null_null = false

[unity.binary]
enable = true
output = "Assets/Tables/Generated"

[unity.xlua]
enable = false

[unity.xml]
enable = false
output = "Assets/Tables/Readable"

[unity.json]
enable = false
output = "Assets/Tables/Readable"

[unity.protobuf]
enable = false
\`\`\`

## Создайте свою первую конфигурационную таблицу

### Структура таблицы Excel

XCell использует определённую структуру таблицы Excel, где первые 3 строки являются заголовками, а данные начинаются с 4-й строки:

| Номер строки | Назначение | Описание |
|------------|---------|-------------|
| 1 | Имя поля | Имена полей конфигурационной таблицы |
| 2 | Тип данных | Типы данных для полей |
| 3 | Комментарий | Описательный текст для полей |
| 4+ | Строки данных | Фактические конфигурационные данные |

### Пример таблицы

Создайте таблицу \`Tables/Hero.xlsx\`:

| id | name | hp | attack | is_boss |
|----|------|----|--------|---------|
| int | string | int | int | bool |
| ID героя | Имя героя | Очки здоровья | Сила атаки | Является боссом |
| 1 | Рыцарь | 1000 | 100 | false |
| 2 | Маг | 800 | 150 | false |
| 3 | Дракон | 5000 | 500 | true |

## Запуск XCell

### Базовая команда

Откройте командную строку в корневой директории проекта и выполните:

\`\`\`bash
xcell.exe
\`\`\`

XCell автоматически:
1. Сканирует все таблицы Excel в текущей директории
2. Проверяет данные таблиц
3. Генерирует соответствующий код C# и бинарные файлы данных

### Параметры командной строки

\`\`\`bash
xcell.exe [OPTIONS] [COMMAND]
\`\`\`

#### Команды

- \`check\`: Проверить конфигурационные таблицы без экспорта файлов
- \`clear\`: Очистить базу данных и кэш

#### Параметры

- \`--workspace <WORKSPACE>\`: Указать рабочую директорию вручную, по умолчанию используется текущая директория
- \`-w, --watch\`: Включить режим наблюдения, обновлять только соответствующие файлы при обнаружении изменений
- \`--disable-xml\`: Принудительно отключить генерацию XML
- \`--disable-json\`: Принудительно отключить генерацию JSON
- \`-h, --help\`: Показать справку
- \`-V, --version\`: Показать версию

### Примеры использования

#### Проверка конфигурационных таблиц

\`\`\`bash
xcell.exe check
\`\`\`

#### Включение режима наблюдения

\`\`\`bash
xcell.exe --watch
\`\`\`

#### Очистка кэша

\`\`\`bash
xcell.exe clear
\`\`\`

## Просмотр результатов генерации

После успешного выполнения вы увидите следующие сгенерированные файлы:

\`\`\`
MyProject/
├── Assets/
│   ├── Scripts/DataTable/Generated/
│   │   ├── HeroTable.cs
│   │   └── DataTableManager.cs
│   └── Tables/Generated/
│       └── HeroTable.bytes
\`\`\`

### Пример сгенерированного кода C#

\`HeroTable.cs\` будет содержать примерно следующее:

\`\`\`csharp
namespace DataTable.Generated
{
    public partial class HeroTable
    {
        public readonly Dictionary<int, HeroElement> dict = new();

        public HeroElement GetElement(int id)
        {
            return dict.TryGetValue(id, out var item) ? item : null;
        }
    }

    public partial class HeroElement
    {
        public int id;
        public string name;
        public int hp;
        public int attack;
        public bool is_boss;
    }
}
\`\`\`

## Следующие шаги

- Ознакомьтесь с [Индексом вариантов использования](use-cases/index.md) для более конкретных примеров
- Пользователи Unity могут обратиться к документации [Интеграция с Unity](use-cases/unity-integration.md)
`);

write('overview/index.md', `# Возможности XCell

## Типы таблиц

| Тип | Описание |
| ---- | ----------- |
| Таблица dict | Строковый первичный ключ, наиболее распространённая форма конфигурации |
| Таблица list | Целочисленный первичный ключ, последовательный доступ |
| Таблица enum | Тип перечисления с дополнительными данными |
| Таблица class | Глобальный класс конфигурации, паттерн одиночка |
| Таблица language | Поддержка многоязычности |

## Цели генерации кода

| Цель | Язык/Формат | Статус |
| ------ | --------------- | ------ |
| Unity | C# | ✅ Реализовано |
| Cocos | TypeScript + JSON | ✅ Реализовано |
| JSON | Данные JSON | ✅ Реализовано |
| Binary | Бинарные данные | ✅ Реализовано |
| Dejavu | Шаблонизатор | ✅ Реализовано |

## Система типов

### Базовые типы

| Тип | Описание |
| ---- | ----------- |
| \`bool\` | Логическое значение |
| \`i8\`, \`i16\`, \`i32\`, \`i64\` | Знаковые целые числа |
| \`u8\`, \`u16\`, \`u32\`, \`u64\` | Беззнаковые целые числа |
| \`f32\`, \`f64\` | Числа с плавающей точкой |
| \`string\` | Строка |

### Составные типы

| Тип | Описание |
| ---- | ----------- |
| \`[T]\` | Динамический массив |
| \`[T; N]\` | Статический массив |
| \`Vec<T>\` | Вектор/Список |
| \`vec2\`, \`vec3\`, \`vec4\` | Векторные типы |
| \`HashMap<K, V>\` | Тип словаря |

### Специальные типы

| Тип | Описание |
| ---- | ----------- |
| \`color\` | Тип цвета |
| \`datetime\`, \`time\`, \`date\` | Типы времени |
| \`&T\` | Ссылочный тип |

## Функция слияния таблиц

- **Соглашение об именовании**: Имена с подчёркиваниями сливаются автоматически, например \`Item_Weapon\` + \`Item_Armor\` → \`Item\`
- **Правила слияния**: Таблицы с одинаковой структурой сливаются автоматически, дублирование ID вызывает ошибки
- **Зарезервированное имя таблицы**: \`Language\` является зарезервированным именем таблицы

## Система конфигурации

- **Конфигурация проекта**: \`XCell.toml\` в корне проекта
- **Конфигурация таблицы**: Файл \`.toml\` с тем же именем
- **Маппинг строк**: Поддержка миграции устаревших таблиц

## Архитектура

\`\`\`
xcell-provider (Чтение/Запись таблиц)
    → xcell-analyzer (Анализ таблиц)
    → xcell-generator (Генерация кода)
    → xcell (CLI-инструмент)
\`\`\`

## Основные модули

| Модуль | Функция |
| ------ | -------- |
| xcell-types | Определения системы типов |
| xcell-provider | Интерфейсы чтения/записи таблиц |
| xcell-parser | Синтаксический анализатор |
| xcell-analyzer | Анализатор таблиц |
| xcell-config | Управление конфигурацией |
| xcell-generator | Генератор кода |
| xcell-plugin | Система плагинов |
| xcell | Инструмент командной строки |
`);

write('overview/class.md', `# Таблица Class

Таблицы Class используются для определения глобальных классов конфигурации, подходящих для сценариев конфигурации-одиночки.

## Соглашения

- **Первая строка, первый столбец — маркер \`@class\`**
- **Генерирует класс-одиночку**
- **Сгенерированное имя по умолчанию совпадает с именем файла, может быть указано через \`@class Name\`**

## Способ маркировки

Используйте маркер \`@class\` в первой строке, первом столбце.

| @class      | @comment | @type  | @default |
| ----------- | -------- | ------ | -------- |
| max\\_hp     | Максимальное здоровье   | i32    | 100      |
| user\\_name  | Имя игрока  | string | Игрок   |
| move\\_speed | Скорость движения   | f32    | 5.0      |

## Генерация кода

### TypeScript (Cocos, Laya)

\`\`\`typescript
export class GameConfig {
    public static maxHp: number = 100;
    public static userName: string = "Player";
    public static moveSpeed: number = 5.0;
}
\`\`\`

### C# (Unity, Godot)

\`\`\`csharp
public static class GameConfig
{
    public static int MaxHp = 100;
    public static string UserName = "Player";
    public static float MoveSpeed = 5.0f;
}
\`\`\`

## Варианты использования

- Глобальная конфигурация игры (например, конфигурация уровней, конфигурация сложности)
- Настройки приложения
- Конфигурация системных параметров
`);

write('overview/dict.md', `# Таблица Dict

Dict — наиболее часто используемая форма конфигурации. Если нет специального маркера типа, по умолчанию используется тип Dict. Dict использует строку в качестве первичного ключа, подходит для данных, к которым нужен доступ по имени.

## Соглашения

- **Первый столбец считается первичным ключом**
- **Тип Dict по умолчанию, явная маркировка не требуется**
- **Можно использовать маркер \`@dict\` для явного объявления**
- **Сгенерированное имя по умолчанию совпадает с именем файла, может быть указано через \`@dict Name\`**

## Способ маркировки

По умолчанию маркировка не требуется, первый столбец автоматически распознаётся как строковый первичный ключ:

| quality_key | item_icon | display_color |
|-------------|-----------|---------------|
| string | string | string |
| common | item_01.png | #FFFFFF |
| rare | item_02.png | #00FFFF |
| epic | item_03.png | #FF00FF |

Также можно явно использовать маркер \`@dict\` в первой строке, первом столбце:

| @dict | item_icon | display_color |
|-------|-----------|---------------|
| quality_key | string | string |
| common | item_01.png | #FFFFFF |
| rare | item_02.png | #00FFFF |
| epic | item_03.png | #FF00FF |

## Генерация кода

### TypeScript (Cocos, Laya)

\`\`\`typescript
export class ItemQuality {
    public qualityKey: string;
    public itemIcon: string;
    public displayColor: string;
}

export class ItemQualityTable {
    private _items: Map<string, ItemQuality>;
}
\`\`\`

### C# (Unity, Godot)

\`\`\`csharp
public class ItemQuality
{
    public string QualityKey;
    public string ItemIcon;
    public string DisplayColor;
}

public class ItemQualityTable
{
    private Dictionary<string, ItemQuality> _items;
}
\`\`\`

## Варианты использования

Конфигурация качества, конфигурация UI, конфигурация звуковых эффектов и другие данные, требующие доступа по имени.
`);

write('overview/enum.md', `# Таблица Enum

Таблицы перечислений используются для определения типов перечислений, а также могут прикреплять дополнительные данные к каждому значению перечисления.

## Соглашения

- **Первый столбец считается первичным ключом**
- **Используйте маркер \`@enum\` для явного объявления типа Enum**
- **Сгенерированное имя по умолчанию совпадает с именем файла, может быть указано через \`@enum Name\`**

## Структура хранения

Таблицы Enum хранятся как **enum + статические поля/статические методы**, получая дополнительные оптимизации.

> ⚠️ Если программа не имеет особых требований, рекомендуется использовать тип dict по умолчанию.

## Способ маркировки

Используйте маркер \`@enum\` в первой строке, первом столбце:

| @enum | Comment | Icon |
| ----- | ------- | ---- |
| name | comment | icon |
| text | utf8 | utf8 |
| Norma | Обычное качество | icon_01.png |
| Rare | Редкое качество | icon_02.png |
| Epic | Эпическое качество | icon_03.png |
| Super | Легендарное качество | icon_04.png |

## Генерация кода

### TypeScript (Cocos, Laya)

\`\`\`typescript
/**
 * Интерфейс качества
 */
export interface Quality {
    /**
     * ID качества
     */
    id: number;
    /**
     * Название качества
     */
    name: string;
    /**
     * Комментарий качества
     */
    comment: string;
    /**
     * Иконка качества
     */
    icon: string;
}

/**
 * Перечисление качества
 */
export const Quality = {
    /**
     * Обычное качество
     */
    Norma: {
        id: 0,
        name: "Norma",
        comment: "Обычное качество",
        icon: "icon_01.png"
    },
    /**
     * Редкое качество
     */
    Rare: {
        id: 1,
        name: "Rare",
        comment: "Редкое качество",
        icon: "icon_02.png"
    },
    /**
     * Эпическое качество
     */
    Epic: {
        id: 2,
        name: "Epic",
        comment: "Эпическое качество",
        icon: "icon_03.png"
    },
    /**
     * Легендарное качество
     */
    Super: {
        id: 3,
        name: "Super",
        comment: "Легендарное качество",
        icon: "icon_04.png"
    }
} as const as Record<string, Quality>;
\`\`\`

### C# (Unity, Godot)

\`\`\`csharp
/// <summary>
/// Перечисление качества
/// </summary>
public enum Quality
{
    /// <summary>
    /// Обычное качество
    /// </summary>
    Norma = 0,
    /// <summary>
    /// Редкое качество
    /// </summary>
    Rare = 1,
    /// <summary>
    /// Эпическое качество
    /// </summary>
    Epic = 2,
    /// <summary>
    /// Легендарное качество
    /// </summary>
    Super = 3
}

/// <summary>
/// Расширенные данные качества
/// </summary>
public static class QualityExtension
{
    private static readonly QualityData[] _data = new QualityData[]
    {
        new QualityData { Id = 0, Name = "Norma", Comment = "Обычное качество", Icon = "icon_01.png" },
        new QualityData { Id = 1, Name = "Rare", Comment = "Редкое качество", Icon = "icon_02.png" },
        new QualityData { Id = 2, Name = "Epic", Comment = "Эпическое качество", Icon = "icon_03.png" },
        new QualityData { Id = 3, Name = "Super", Comment = "Легендарное качество", Icon = "icon_04.png" }
    };

    public static QualityData GetData(this Quality quality)
    {
        return _data[(int)quality];
    }
}

public class QualityData
{
    public int Id;
    public string Name;
    public string Comment;
    public string Icon;
}
\`\`\`

## Варианты использования

- Определения качества предметов
- Определения статусов персонажей
- Определения типов событий
- Любые сценарии, требующие перечислений с дополнительными данными
`);

write('overview/language.md', `# Таблица Language

Таблицы языков используются для управления многоязычным текстом. XCell обеспечивает полную поддержку интернационализации.

## Соглашения

- **Первая строка, первый столбец — маркер \`@language\`**
- **Необязательное поле \`@group\` для группировки**
- **Остальные столбцы — идентификаторы языков**
- **Сгенерированное имя по умолчанию совпадает с именем файла, может быть указано через \`@language Name\`**

## Способ маркировки

Используйте маркер \`@language\` в первой строке, первом столбце, необязательный \`@group\`, остальные столбцы — идентификаторы языков:

| @language | @group | zh_cn | en_us | ja_jp |
|-----------|--------|-------|-------|-------|
| Ui_Start | ui | Начало | Start | スタート |
| Ui_Settings | ui | Настройки | Settings | 設定 |
| Ui_Exit | ui | Выход | Exit | 終了 |

## Генерация кода

### TypeScript (Cocos, Laya)

\`\`\`typescript
export class LanguageTable {
    private static _items: Map<string, string>;

    public static get(key: string): string {
        return this._items.get(key) ?? key;
    }
}
\`\`\`

### C# (Unity, Godot)

\`\`\`csharp
public static class LanguageTable
{
    private static Dictionary<string, string> _items;

    public static string Get(string key)
    {
        return _items.TryGetValue(key, out var value) ? value : key;
    }
}
\`\`\`

## Варианты использования

- Многоязычная поддержка игрового UI
- Интернационализация приложений
- Системы многоязычной документации
- Любые проекты, требующие многоязычной поддержки
`);

write('overview/list.md', `# Таблица List

List — это форма конфигурации, использующая целочисленный ID в качестве первичного ключа, подходящая для данных, к которым нужен последовательный доступ или доступ по числовому ID.

## Соглашения

- **Первый столбец считается первичным ключом**
- **Используйте маркер \`@list\` для явного объявления типа List**
- **Сгенерированное имя по умолчанию совпадает с именем файла, может быть указано через \`@list Name\`**

## Структура хранения

Таблицы List хранятся как **Массив**, производительность find_key составляет **O(log n)**.

> ⚠️ Если программа не имеет особых требований, рекомендуется использовать таблицу dict по умолчанию.

## Способ маркировки

Используйте маркер \`@list\` в первой строке, первом столбце:

| @list | monster_name | hit_points | attack_damage |
|-------|--------------|------------|---------------|
| id | string | f32 | i32 |
| 1 | Слизень | 50 | 5 |
| 2 | Гоблин | 80 | 10 |
| 3 | Скелет | 100 | 15 |

## Генерация кода

### TypeScript (Cocos, Laya)

\`\`\`typescript
export class Monster {
    public id: number;
    public monsterName: string;
    public hitPoints: number;
    public attackDamage: number;
}

export class MonsterTable {
    private _items: Monster[];
}
\`\`\`

### C# (Unity, Godot)

\`\`\`csharp
public class Monster
{
    public int Id;
    public string MonsterName;
    public float HitPoints;
    public float AttackDamage;
}

public class MonsterTable
{
    private Monster[] _items;
}
\`\`\`

## Варианты использования

Конфигурация монстров, конфигурация предметов, конфигурация навыков и другие данные, требующие последовательного доступа или доступа по числовому ID.
`);

write('overview/merge.md', `# Правила слияния таблиц

XCell поддерживает слияние нескольких таблиц в одну. Через правила слияния вы можете гибко управлять сложными структурами данных.

## Соглашение об именовании

- **Именование PascalCase** (например, \`ItemWeapon\`, \`ItemArmor\`): Независимые таблицы, не участвующие в слиянии
- **Именование с подчёркиванием** (например, \`Item_Weapon\`, \`Item_Armor\`): Автоматически сливаются в таблицу \`Item\`

## Пример

| Имя файла | Поведение |
|----------|----------|
| \`ItemWeapon.xlsx\` | Независимая таблица, генерирует \`ItemWeapon\` |
| \`ItemArmor.xlsx\` | Независимая таблица, генерирует \`ItemArmor\` |
| \`Item_Weapon.xlsx\` | Сливается в \`Item\` |
| \`Item_Armor.xlsx\` | Сливается в \`Item\` |
| \`Item_Consumable.xlsx\` | Сливается в \`Item\` |

## Логика слияния

- Таблицы с одинаковой структурой будут объединены
- **Дублирование ID вызовет ошибки** (порядок слияния зависит от хранения на диске, не является фиксированным)
- Окончательный вывод будет отсортирован по ID

## Варианты использования

### Сценарий: Управление таблицами по типу

Предположим, у вас есть следующие таблицы предметов:
- \`Item_Weapon.xlsx\` - Конфигурация оружия
- \`Item_Armor.xlsx\` - Конфигурация брони
- \`Item_Consumable.xlsx\` - Конфигурация расходуемых предметов

Они автоматически сольются в таблицу \`Item\`.

## Примечания

- Объединяемые таблицы должны иметь одинаковую структуру (одинаковые имена столбцов и типы)
- При конфликте первичных ключей данные из более позднего файла перезапишут данные из более раннего
- Рекомендуется создавать резервные копии исходных файлов перед выполнением операций слияния
- **\`Language\` — зарезервированное имя таблицы**, все таблицы языков сливаются в Language, не используйте это имя
`);

write('advanced/index.md', `# Расширенные темы

Этот раздел предназначен для разработчиков, которые имеют базовое понимание XCell и хотят узнать больше о его внутренних механизмах, расширенных функциях или реализовать расширенные конфигурации.

## Структура документации

### Система типов
- [type-system.md](type-system.md) - Документация системы типов
  - Базовые типы (целые числа, числа с плавающей точкой, логические, строки)
  - Составные типы (массивы, векторы, словари, кортежи)
  - Специальные типы (цвет, время)
  - Пользовательские типы (перечисления, структуры)
  - Преобразование и валидация типов

### Ограничения полей
- [key-field.md](key-field.md) - Документация ограничений полей
  - Ограничения уникальности
  - Ограничения первичного ключа
  - Составные ограничения

### Ссылочные типы
- [ref-type.md](ref-type.md) - Документация ссылочных типов
  - Базовый формат
  - Принцип работы
  - Варианты использования
  - Валидация ссылок

### Мета-атрибуты
- [meta-data.md](meta-data.md) - Документация мета-атрибутов
  - Базовые мета-атрибуты (var, type, default, field, client, server, meta)
  - Маркеры типов таблиц (class, enum, table, language)
  - Правила использования и примеры

### Конфигурация
- [config.md](config.md) - Документация файлов конфигурации
  - Конфигурация проекта
  - Конфигурация таблиц
  - Маппинг строк

### Расширяемость
- [extensibility.md](extensibility.md) - Документация расширяемости
  - Пользовательская система типов
  - Расширение генераторов кода (поддержка любого языка программирования)
  - Создание экспортёров для других игровых движков
  - Руководство по разработке плагинов

## Поддержка нескольких движков

Философия дизайна XCell — предоставлять отличные решения для управления конфигурационными таблицами для всех игровых движков:

- **Unity (C#)**: Полная встроенная поддержка
- **Cocos Creator**: Через загрузчики JSON/XML + TypeScript/Lua
- **Godot**: Через загрузчики JSON + GDScript
- **Unreal Engine**: Через загрузчики бинарных данных + C++
- **Пользовательские движки**: Создание пользовательских экспортёров через документацию по расширяемости

## Предварительные требования

Перед чтением этого раздела рекомендуется:

1. Быть знакомым с базовым использованием XCell
2. Понимать базовую структуру файла конфигурации проекта \`XCell.toml\`
3. Иметь базовые навыки программирования на целевых языках (Rust, C#, C++, Python, Lua, TypeScript и др.)
`);

write('advanced/advanced-features.md', `# Документация расширенных функций

Этот документ подробно описывает расширенные функции инструмента управления конфигурационными таблицами XCell, включая расширенные параметры конфигурации, подробные правила слияния таблиц и советы по оптимизации производительности.

## Содержание

1. [Расширенные параметры конфигурации](#расширенные-параметры-конфигурации)
2. [Подробные правила слияния таблиц](#подробные-правила-слияния-таблиц)
3. [Советы по оптимизации производительности](#советы-по-оптимизации-производительности)
4. [Интеграция с движками](#интеграция-с-движками)

---

## Расширенные параметры конфигурации

Файл конфигурации XCell \`XCell.toml\` предоставляет богатые параметры конфигурации, позволяя гибко настраивать поведение инструмента.

### Базовая структура конфигурации

Полный файл конфигурации \`XCell.toml\` содержит следующие основные части:

\`\`\`toml
version = "0.0.0"

exclude = ""
include = "*.xlsx"

line.field = 1
line.type = 2
line.comment = 3
line.data = 4

[type.bool]
accept = ["true", "√"]
reject = ["false", "x"]

[type.string]

[merge]
\`\`\`

### Конфигурация управления версиями

\`\`\`toml
version = "0.0.0"
\`\`\`

- **version**: Номер текущей версии проекта, используется для идентификации версии сгенерированного кода и файлов данных

### Конфигурация включения/исключения файлов

\`\`\`toml
exclude = ""
include = "*.xlsx"
\`\`\`

- **include**: Шаблон файлов Excel для включения, поддерживает подстановочные знаки
- **exclude**: Шаблон файлов Excel для исключения, приоритет ниже, чем у include

Пример:
\`\`\`toml
include = "tables/**/*.xlsx"
exclude = "tables/temp/*.xlsx"
\`\`\`

### Конфигурация строк таблицы

\`\`\`toml
line.field = 1
line.type = 2
line.comment = 3
line.data = 4
\`\`\`

- **line.field**: Номер строки, где расположены имена полей (начиная с 1)
- **line.type**: Номер строки, где расположены объявления типов
- **line.comment**: Номер строки, где расположены комментарии
- **line.data**: Номер строки, с которой начинаются данные

### Конфигурация типов

#### Конфигурация логического типа

\`\`\`toml
[type.bool]
accept = ["true", "√"]
reject = ["false", "x"]
\`\`\`

- **accept**: Список значений, распознаваемых как true
- **reject**: Список значений, распознаваемых как false

#### Конфигурация строкового типа

\`\`\`toml
[type.string]
# Расширяемая конфигурация
\`\`\`

---

## Подробные правила слияния таблиц

Правила слияния таблиц позволяют объединять несколько таблиц Excel в одну, поддерживая слияние по строкам или столбцам.

### Конфигурация правил слияния

Правила слияния настраиваются в секции \`[merge]\`, каждое правило использует уникальный номер в качестве идентификатора.

\`\`\`toml
[merge.10001]
mode = "row"
input = "Language_CN*"
target = "Language_CN"

[merge.10002]
mode = "row"
input = "Language_EN*"
target = "Language_EN"

[merge.20001]
mode = "column"
input = "Language*"
target = "Language"
\`\`\`

### Режимы слияния

#### Режим слияния по строкам (row)

\`\`\`toml
mode = "row"
\`\`\`

Слияние по строкам объединяет строки данных из нескольких таблиц в одну. Подходит для:
- Больших таблиц, которые нужно разделить для управления
- Раздельного управления многоязычными данными

Пример структуры директории:
\`\`\`
LanguageTable/
  - CN/
    - Language_CN_UI.xlsx
    - Language_CN_Item.xlsx
  - EN/
    - Language_EN_UI.xlsx
    - Language_EN_Item.xlsx
\`\`\`

#### Режим слияния по столбцам (column)

\`\`\`toml
mode = "column"
\`\`\`

Слияние по столбцам объединяет столбцы из нескольких таблиц в одну. Подходит для:
- Распределения данных столбцов по разным файлам
- Совместной работы разных команд над разными частями одной таблицы

### Параметры правил слияния

- **mode**: Режим слияния, \`row\` или \`column\`
- **input**: Шаблон входных файлов, поддерживает подстановочные знаки
- **target**: Имя целевой таблицы

### Порядок выполнения слияния

Номера правил определяют порядок выполнения слияния, меньшие номера выполняются раньше. Рекомендации:
- Слияние по строкам использует диапазон 10000-19999
- Слияние по столбцам использует диапазон 20000-29999
- Другие пользовательские правила используют более высокие номера

---

## Советы по оптимизации производительности

### Оптимизация обработки больших таблиц

#### 1. Разумное разделение больших таблиц

- Разделяйте большие таблицы по функциям или модулям
- Используйте правила слияния по строкам для объединения при сборке
- Поддерживайте удобство сопровождения во время разработки и производительность во время выполнения

#### 2. Использование бинарного формата

- Бинарный формат имеет самую высокую скорость загрузки
- Бинарный формат рекомендуется для производственных сред
- XML/JSON можно использовать в средах разработки для отладки

### Оптимизация инкрементальных обновлений

#### 1. Режим наблюдения

Используйте режим наблюдения:

\`\`\`bash
xcell.exe --watch
\`\`\`

Особенности режима наблюдения:
- Регенерация только изменённых файлов
- Значительное повышение эффективности разработки
- Поддержка предварительного просмотра в реальном времени

#### 2. Разумная конфигурация наблюдения

Настройте разумные шаблоны include/exclude для уменьшения количества наблюдаемых файлов.

### Оптимизация памяти

#### 1. Загрузка только необходимых таблиц

В соответствии с API различных движков загружайте только таблицы, необходимые для текущей сцены или функции, избегайте загрузки всех таблиц сразу.

#### 2. Своевременная выгрузка

Когда определённые таблицы больше не нужны, своевременно выгружайте их для освобождения памяти.

### Оптимизация сборки

#### 1. Параллельная обработка

XCell поддерживает параллельную обработку нескольких файлов, значительно ускоряя сборку.

#### 2. Механизм кэширования

Разумно используйте кэширование, чтобы избежать повторной обработки.

---

## Интеграция с движками

XCell поддерживает интеграцию с несколькими игровыми движками, подробности см. в следующей документации:

- [Интеграция с Unity](./unity.md) - Подробности интеграции XCell с движком Unity
- [Интеграция с Cocos](./cocos.md) - Подробности интеграции XCell с движком Cocos

---

## Резюме

XCell предоставляет богатые расширенные функции, через разумную конфигурацию и оптимизацию может удовлетворить потребности различных сложных проектов.

- Используйте расширенные параметры конфигурации для настройки поведения XCell
- Используйте правила слияния для управления сложными таблицами
- Повышайте эффективность во время выполнения и сборки через оптимизацию производительности
- Повышайте эффективность разработки через интеграцию с движками

Если у вас есть вопросы или предложения, не стесняйтесь отправлять Issue или PR!
`);

write('advanced/config.md', `# Файлы конфигурации

XCell использует файлы конфигурации в формате TOML для управления настройками проекта. Файл конфигурации называется \`XCell.toml\` и расположен в корневой директории проекта.

## Базовая конфигурация

| Параметр конфигурации | Тип | Описание | Значение по умолчанию |
|--------------------|------|-------------|---------------|
| version | string | Номер версии файла конфигурации | "0.0.0" |
| include | string | Шаблон пути к файлам Excel для включения (наивысший приоритет) | "*.xlsx" |
| exclude | string | Шаблон пути к файлам Excel для исключения (приоритет ниже, чем у include) | "" |

### Конфигурация строк (line)

Определяет номера строк, где расположена различная информация в таблице (начиная с 1).

| Параметр конфигурации | Тип | Описание | Значение по умолчанию |
|--------------------|------|-------------|---------------|
| line.field | int | Строка, где расположены имена полей | 1 |
| line.type | int | Строка, где расположены типы данных | 2 |
| line.comment | int | Строка, где расположены комментарии | 3 |
| line.data | int | Строка, с которой начинаются данные | 4 |

#### Миграция устаревших таблиц

Формат таблицы XCell по умолчанию:

| Номер строки | Содержимое |
|------------|---------|
| Строка 1 | Комментарии к полям |
| Строка 2 | Имена полей |
| Строка 3 | Типы полей |
| Строка 4+ | Строки данных |

Если формат вашей устаревшей таблицы отличается, вы можете настроить его через маппинг строк. Например, если формат устаревшей таблицы:

| Номер строки | Содержимое |
|------------|---------|
| Строка 1 | Имена полей |
| Строка 2 | Типы полей |
| Строка 3+ | Строки данных |

Настройте следующим образом:

\`\`\`toml
line.field = 1
line.type = 2
line.comment = 0  # Нет строки комментариев
line.data = 3
\`\`\`

> Примечание: \`line.comment = 0\` означает отсутствие строки комментариев.

### Конфигурация разбора типов (type)

Настройте правила разбора для различных типов данных.

#### Логический тип (bool)

| Параметр конфигурации | Тип | Описание |
|--------------------|------|-------------|
| type.bool.accept | array[string] | Список значений, принимаемых как true |
| type.bool.reject | array[string] | Список значений, принимаемых как false |

Пример:
\`\`\`toml
[type.bool]
accept = ["true", "√", "是", "1"]
reject = ["false", "x", "否", "0"]
\`\`\`

### Конфигурация генерации кода Unity (unity)

Настройте параметры генерации кода C#.

| Параметр конфигурации | Тип | Описание | Значение по умолчанию |
|--------------------|------|-------------|---------------|
| unity.enable | bool | Включить ли генерацию кода Unity | true |
| unity.project | string | Путь к проекту Unity | "../" |
| unity.output | string | Директория вывода кода | "Assets/Scripts/DataTable/Generated" |
| unity.namespace | string | Пространство имён сгенерированного кода | "DataTable.Generated" |
| unity.manager | string | Имя класса менеджера | "DataTableManager" |
| unity.suffix_table | string | Суффикс класса таблицы | "Table" |
| unity.suffix_element | string | Суффикс класса элемента | "Element" |
| unity.support_clone | bool | Поддерживать ли клонирование | true |
| unity.legacy_using | bool | Использовать ли устаревший using | false |
| unity.legacy_null_null | bool | Использовать ли устаревшую обработку null | false |

### Конфигурация формата вывода данных

Настройте вывод файлов данных в различных форматах.

#### Бинарный формат

| Параметр конфигурации | Тип | Описание | Значение по умолчанию |
|--------------------|------|-------------|---------------|
| unity.binary.enable | bool | Включить ли вывод в бинарном формате | true |
| unity.binary.output | string | Директория вывода бинарных файлов | "Assets/Tables/Generated" |

#### Формат XML

| Параметр конфигурации | Тип | Описание | Значение по умолчанию |
|--------------------|------|-------------|---------------|
| unity.xml.enable | bool | Включить ли вывод в формате XML | false |
| unity.xml.output | string | Директория вывода XML файлов | "Assets/Tables/Readable" |

#### Формат JSON

| Параметр конфигурации | Тип | Описание | Значение по умолчанию |
|--------------------|------|-------------|---------------|
| unity.json.enable | bool | Включить ли вывод в формате JSON | false |
| unity.json.output | string | Директория вывода JSON файлов | "Assets/Tables/Readable" |

#### Другие форматы

- **xlua**: Генерация кода Lua
- **protobuf**: Вывод в формате Protobuf

## Инструкция по использованию

1. Создайте файл \`XCell.toml\` в корневой директории проекта
2. Измените параметры конфигурации по необходимости
3. Инструмент XCell автоматически загрузит конфигурацию при запуске
4. Конфигурация таблицы может переопределять глобальную конфигурацию (создайте файл \`.toml\` с тем же именем)
`);

write('advanced/extensibility.md', `# Документация расширяемости

Этот документ описывает, как расширить функциональность инструмента управления конфигурационными таблицами XCell, включая пользовательскую систему типов, расширение генераторов кода и разработку плагинов.

## Содержание

1. [Пользовательская система типов](#пользовательская-система-типов)
2. [Расширение генераторов кода](#расширение-генераторов-кода)
3. [Руководство по разработке плагинов](#руководство-по-разработке-плагинов)

***

## Пользовательская система типов

XCell предоставляет гибкую систему типов, поддерживающую множество встроенных типов данных и позволяющую разработчикам создавать новые типы данных.

### Обзор встроенных типов

XCell поддерживает следующие встроенные типы:

| Категория типов | Поддерживаемые типы                                                                                                            |
| ------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Логические       | \`bool\`, \`boolean\`                                                                                                          |
| Целые числа       | \`byte\`/\`i8\`, \`short\`/\`i16\`, \`int\`/\`i32\`, \`long\`/\`i64\`, \`sbyte\`/\`u8\`, \`ushort\`/\`u16\`, \`uint\`/\`u32\`, \`ulong\`/\`u64\`          |
| Десятичные       | \`float\`/\`f32\`, \`double\`/\`f64\`, \`decimal\`/\`d128\`/\`f128\`                                                                     |
| Строковые        | \`string\`                                                                                                                   |
| Специальные типы | \`color\`/\`colour\`, \`color32\`, \`time\`/\`date\`/\`datetime\`                                                                      |
| Векторы/Массивы  | \`v2\`/\`vec2\`, \`v3\`/\`vec3\`, \`v4\`/\`vec4\`, \`q4\`/\`quaternion\`                                                                   |
| Перечисления          | Пользовательские типы перечислений                                                                                                          |

### Архитектура системы типов

Ядро системы типов XCell расположено в модуле \`xcell-types\`, в основном содержит следующие компоненты:

- \`XCellTyped\`: Перечисление типов, определяющее все поддерживаемые типы данных
- \`TypeMetaInfo\`: Метаданные типа, содержащие информацию о конфигурации типа
- \`XCellValue\`: Значение типа, хранящее разобранные данные
- Различные дескрипторы типов: такие как \`IntegerDescription\`, \`DecimalDescription\` и др.

### Шаги реализации пользовательского типа

Чтобы добавить пользовательский тип, выполните следующие шаги:

#### 1. Создание модуля описания типа

Создайте новый модуль типа в директории \`projects/xcell-types/src/\`, например \`my_type/mod.rs\`:

\`\`\`rust
use serde::{Deserialize, Serialize};
use xcell_errors::XResult;
use crate::value::XCellValue;

#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct MyTypeDescription {
    pub default: Option<String>,
}

impl MyTypeDescription {
    pub fn parse_cell(&self, cell: &str) -> XResult<XCellValue> {
        todo!("Реализуйте логику разбора ячеек")
    }
}
\`\`\`

#### 2. Расширение перечисления XCellTyped

Расширьте перечисление \`XCellTyped\` в \`projects/xcell-types/src/typing/mod.rs\`:

\`\`\`rust
pub enum XCellTyped {
    // ... существующие типы ...
    MyType(Box<MyTypeDescription>),
}
\`\`\`

#### 3. Реализация разбора типа

Добавьте логику разбора типа в \`projects/xcell-types/src/typing/parser.rs\`:

\`\`\`rust
impl XCellTyped {
    pub fn parse(input: &str, info: &TypeMetaInfo) -> Self {
        let normed = Self::norm_typing(input);
        match normed.as_str() {
            // ... существующие типы ...
            "mytype" | "my_type" => info.my_type.clone().into(),
            _ => XCellTyped::parse_complex(input, &normed, info),
        }
    }
}
\`\`\`

#### 4. Обновление TypeMetaInfo

Обновите структуру \`TypeMetaInfo\` в \`projects/xcell-types/src/typing/mod.rs\`:

\`\`\`rust
#[derive(Debug, Clone, Default, Serialize)]
pub struct TypeMetaInfo {
    // ... существующие поля ...
    pub my_type: MyTypeDescription,
}
\`\`\`

#### 5. Добавление поддержки генерации кода

Добавьте логику генерации кода для нового типа в \`projects/xcell-types/src/codegen/\`, обеспечив корректные определения типов для целевых языков (например, C#).

***

## Расширение генераторов кода

XCell поддерживает несколько целей генерации кода, включая Unity C#, бинарные файлы, XML, JSON и др. Вы можете расширить эти генераторы или создать новые.

### Архитектура генерации кода

Генерация кода в основном реализована в модуле \`xcell-core/src/codegen/\`:

- \`binary/\`: Генерация бинарного формата
- \`readable/\`: Генерация читаемого формата (XML, JSON)
- \`unity/\`: Генерация кода Unity C#

### Расширение генератора кода Unity

Генератор кода Unity — один из наиболее часто используемых генераторов. Вот шаги для его расширения:

#### 1. Просмотр существующих шаблонов

Генерация кода Unity использует файлы шаблонов, расположенные в директории \`projects/xcell-core/templates/\`:

- \`BuildClass.cs\`: Шлон таблицы Class
- \`BuildDictionary.cs\`: Шаблон таблицы Dictionary
- \`BuildEnumerate.cs\`: Шаблон таблицы Enum
- \`BuildLanguage.cs\`: Шаблон таблицы Language
- \`BuildManager.cs\`: Шаблон менеджера

#### 2. Изменение или создание шаблонов

Измените существующие шаблоны или создайте новые файлы шаблонов по необходимости.

#### 3. Обновление конфигурации UnityCodegen

Обновите конфигурацию в \`projects/xcell-core/src/config/unity/mod.rs\`:

\`\`\`rust
#[derive(Debug, Clone)]
pub struct UnityCodegen {
    // ... существующие поля ...
    pub my_custom_option: bool,
}
\`\`\`

#### 4. Реализация логики генерации

Реализуйте конкретную логику генерации в \`projects/xcell-core/src/codegen/unity/\`.

### Создание нового генератора кода

Чтобы создать совершенно новый генератор кода, выполните следующие шаги:

#### 1. Создание модуля генератора

Создайте новый модуль в директории \`projects/xcell-core/src/codegen/\`, например \`cocos/mod.rs\`:

\`\`\`rust
use xcell_errors::XResult;
use crate::config::ProjectConfig;

pub struct CocosCodegen {
    // Поля конфигурации
}

impl CocosCodegen {
    pub fn write(&self, config: &ProjectConfig) -> XResult<()> {
        todo!("Реализуйте логику генерации кода Cocos")
    }
}
\`\`\`

#### 2. Интеграция в систему конфигурации

Добавьте параметры конфигурации для нового генератора в \`ProjectConfig\`.

#### 3. Подключение к рабочему процессу

Вызовите новый генератор в \`WorkspaceManager::write_unity()\` или аналогичных методах.

***

## Руководство по разработке плагинов

XCell поддерживает расширение функциональности через систему плагинов. Плагины могут добавлять новые типы таблиц, пользовательскую логику валидации или расширять возможности генерации кода.

### Архитектура плагинов

Система плагинов основана на системе типажей Rust, основные интерфейсы включают:

- Типаж обработчика таблиц
- Типаж валидатора
- Типаж генератора кода

### Шаги разработки плагина

#### 1. Создание проекта плагина

Создайте новый проект Rust и добавьте зависимости от \`xcell-core\` и \`xcell-types\`:

\`\`\`toml
[package]
name = "xcell-my-plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
xcell-core = { path = "../xcell-core" }
xcell-types = { path = "../xcell-types" }
\`\`\`

#### 2. Реализация типажа плагина

Реализуйте соответствующие типажи по необходимости. Например, реализуйте пользовательский обработчик таблиц:

\`\`\`rust
use xcell_core::x_table::table::CalamideTable;
use xcell_errors::XResult;

pub struct MyCustomTable;

impl MyCustomTable {
    pub fn confirm(table: &CalamideTable) -> XResult<Self> {
        todo!("Проверьте, соответствует ли таблица пользовательскому формату")
    }

    pub fn perform(&self, workspace: &mut WorkspaceManager) -> XResult<()> {
        todo!("Выполните логику обработки таблицы")
    }
}
\`\`\`

#### 3. Регистрация плагина

Зарегистрируйте ваш плагин в методе \`WorkspaceManager::try_perform_file()\`, чтобы он мог быть распознан и обработан.

### Лучшие практики разработки плагинов

1. **Держите плагины независимыми**: Плагины должны быть максимально независимыми, уменьшая зависимости от внутренней реализации XCell
2. **Предоставляйте параметры конфигурации**: Предоставляйте конфигурацию плагина через \`XCell.toml\`
3. **Обработка ошибок**: Правильно обрабатывайте ошибки, предоставляйте понятные сообщения об ошибках
4. **Документация**: Предоставляйте полную документацию по использованию плагинов
5. **Тестирование**: Пишите достаточное количество тестовых случаев

***

## Резюме

XCell предоставляет мощную расширяемость, позволяя разработчикам настраивать функциональность в соответствии с их потребностями. Независимо от того, добавляете ли вы новые типы данных, расширяете генераторы кода или разрабатываете независимые плагины, модульная архитектура XCell может хорошо поддерживать эти потребности.

Если вы столкнулись с проблемами при расширении, обратитесь к исходному коду проекта или отправьте Issue для получения помощи.
`);

write('advanced/key-field.md', `# Ограничения полей

Ограничения полей — это правила, используемые для ограничения значений полей, обеспечивающие уникальность и целостность данных.

## Ограничение уникальности (unique)

Ограничение уникальности гарантирует, что значения поля не могут дублироваться.

### Способ маркировки

| Сокращение | Нотация мета-атрибута | Описание |
| --------- | ----------------------- | ----------- |
| \`@field_name\` | \`field_name @unique\` | Уникальное поле, значения не могут дублироваться |

### Пример

| @dict | Name | Email |
| ----- | ---- | ----- |
| user\\_id | name | @email |
| string | string | string |
| user\\_001 | John | <john@example.com> |
| user\\_002 | Jane | <jane@example.com> |

- \`@email\` - Значения поля Email не могут дублироваться

## Ограничение первичного ключа (primary)

Первичный ключ — это поле, используемое для уникальной идентификации каждой строки данных в таблице.

### Правило по умолчанию

- **Первый столбец автоматически считается первичным ключом**, дополнительная маркировка не требуется

### Способ маркировки

| Сокращение | Нотация мета-атрибута | Описание |
| --------- | ----------------------- | ----------- |
| \`@@field_name\` | \`field_name @primary\` | Поле первичного ключа, значения уникальны и являются первичным ключом |

### Пример

| @dict | Name |
| ----- | ---- |
| @@item\\_id | name |
| string | string |
| sword\\_001 | Железный меч |
| sword\\_002 | Стальной меч |

- \`@@item_id\` - Поле первичного ключа, значения уникальны

## Составное ограничение

Когда комбинация нескольких полей должна быть уникальной, можно использовать составные ограничения. Добавьте \`@unique(field1, field2)\` после маркера типа:

| @dict @unique(class, level) | Class | Level |
| --------------------------- | ----- | ----- |
| id | class | level |
| string | string | i32 |
| warrior\\_001 | warrior | 10 |
| warrior\\_002 | warrior | 20 |
| mage\\_001 | mage | 10 |

В приведённом выше примере комбинация \`Class + Level\` должна быть уникальной, но отдельные значения class или level могут дублироваться.

> ⚠️ **Предупреждение**: Если используется составное ограничение уникальности, то ссылки \`&T\` на эту таблицу также должны заполнять составные ключи. Некоторые игровые движки не поддерживают составные ключи, используйте с осторожностью.

## Требования к ограничениям

- Значения первичного ключа должны быть уникальными
- Значения первичного ключа не могут быть null
- Каждая таблица может иметь только один первичный ключ
- Значения уникальных полей не могут дублироваться
`);

write('advanced/meta-data.md', `# Мета-атрибуты

Мета-атрибуты используются для добавления дополнительной конфигурационной информации к полям, такой как правила валидации, значения по умолчанию и т.д.

## Базовый формат

Мета-атрибуты начинаются с \`@\` и могут быть написаны в трёх позициях:

| Позиция | Пример |
| -------- | ------- |
| После имени поля | \`id @primary\` |
| После типа | \`i32 @min(1)\` |
| В комментарии Excel | \`@default(100)\` |

> **Рекомендация**: Мета-атрибутов может быть любое количество, рекомендуется писать их единообразно в комментариях ячеек Excel для удобства управления и сопровождения.

## Часто используемые мета-атрибуты

### Мета-атрибуты полей

| Мета-атрибут | Описание | Пример |
| -------------- | ----------- | ------- |
| \`@primary\` | Первичный ключ | \`id @primary\` |
| \`@default(value)\` | Значение по умолчанию | \`level @default(1)\` |
| \`@virtual\` | Виртуальное поле | \`user @virtual\` |

### Мета-атрибуты типов

| Мета-атрибут | Описание | Пример |
| -------------- | ----------- | ------- |
| \`@min(value)\` | Минимальное значение | \`i32 @min(0)\` |
| \`@max(value)\` | Максимальное значение | \`i32 @max(100)\` |
| \`@range(min, max)\` | Диапазон | \`i32 @range(1, 100)\` |

### Мета-атрибуты в комментариях

Мета-атрибуты можно писать в комментариях ячеек Excel:

\`\`\`
@default(100)
@min(1)
@max(9999)
\`\`\`

## Виртуальные поля

Виртуальные поля — это специальные поля, которые не хранят фактические данные, а получают значения путём ссылки на данные из других таблиц.

### Пример

| @dict | Name | Quality ID | Quality |
| ----- | ---- | ---------- | ------- |
| item_id | name | quality_id | quality @virtual |
| string | string | &Quality | Quality |
| sword_001 | Железный меч | common | |
| sword_002 | Стальной меч | rare | |

- \`quality_id\` — фактически хранимое поле, тип \`&Quality\`
- \`quality\` — виртуальное поле, помеченное \`@virtual\`, тип — имя целевой таблицы \`Quality\`

## Вычисляемые свойства

Вычисляемые свойства — это поля, получаемые через вычисление выражений, не требующие хранения фактических данных.

### Пример

| @dict | Base Attack | Enhance Level | Total Attack |
| ----- | ----------- | ------------- | ------------ |
| item_id | base_atk | enhance | total_atk @computed |
| string | i32 | i32 | i32 |
| sword_001 | 100 | 5 | base_atk * (1 + enhance * 0.1) |

- \`total_atk\` — вычисляемое свойство, помеченное \`@computed\`
- Выражения могут ссылаться на другие поля в той же таблице

## Примеры

### Поле с валидацией

| @dict | Level | Gold |
| ----- | ----- | ---- |
| user_id | level | gold |
| string | i32 @range(1, 100) | i32 @min(0) |
| user_001 | 10 | 1000 |
| user_002 | 50 | 5000 |

### Поле со значением по умолчанию

| @dict | Name | Quality |
| ----- | ---- | ------- |
| item_id | name | quality |
| string | string | string @default(common) |
| sword_001 | Железный меч | |
| sword_002 | Стальной меч | rare |

## Мета-атрибуты и генерация кода

Мета-атрибуты влияют на сгенерированный код:

### Атрибуты валидации

\`\`\`
level i32 @range(1, 100)
\`\`\`

Сгенерированный код C#:

\`\`\`csharp
[Range(1, 100)]
public int Level { get; set; }
\`\`\`

### Значение по умолчанию

\`\`\`
quality string @default(common)
\`\`\`

Сгенерированный код C#:

\`\`\`csharp
public string Quality { get; set; } = "common";
\`\`\`

## Примечания

- Мета-атрибуты начинаются с \`@\`
- Могут быть написаны в имени поля, типе или комментарии Excel
- Несколько мета-атрибутов можно комбинировать
- Некоторые мета-атрибуты влияют на генерацию кода и валидацию данных
`);

write('advanced/ref-type.md', `# Ссылочные типы

## Зачем нужны ссылочные типы?

При настройке игровых данных часто возникают следующие проблемы:

**Неправильный ID**

В таблице предметов вы указали ID качества \`comon\`, но в таблице качества фактически \`common\`, пропущена одна буква \`m\`. Такого рода опечатки трудно обнаружить, и проблемы возникнут при запуске игры.

**ID изменён, ссылка не синхронизирована**

Таблица качества изменила \`common\` на \`normal\`, но таблица предметов всё ещё использует \`common\`. В результате предметы не могут найти соответствующую конфигурацию качества.

---

Ссылочные типы предназначены для решения этих проблем. XCell автоматически проверяет правильность ссылок, обеспечивая:
- Указанные ID должны существовать в целевой таблице
- Если ID не существует, будет немедленно выдана ошибка

## Базовый формат

- \`&T\` - Ссылка на таблицу T

## Что такое ссылка?

Проще говоря, ссылка — это «связь, указывающая на строку данных в другой таблице».

Например: В таблице предметов вам нужно записать «какому качеству принадлежит этот предмет», в этом случае вы можете использовать ссылочный тип для указания на таблицу качества.

## Пример

### Шаг 1: Создание таблицы качества

Сначала создайте таблицу качества:

| @dict | Color | Description |
| ----- | ----- | ----------- |
| quality_id | color | desc |
| string | string | string |
| common | #FFFFFF | Обычное качество |
| rare | #00FFFF | Редкое качество |
| epic | #FF00FF | Эпическое качество |

### Шаг 2: Создание таблицы предметов (ссылка на таблицу качества)

Поле \`quality_id\` в таблице предметов использует тип \`&Quality\`, указывая на то, что оно ссылается на таблицу качества:

| @dict | Name | Quality | Attack |
| ----- | ---- | ------- | ------ |
| item_id | name | quality_id | attack |
| string | string | &Quality | i32 |
| sword_001 | Железный меч | common | 10 |
| sword_002 | Стальной меч | rare | 50 |
| sword_003 | Драконий меч | epic | 200 |

## Частые варианты использования

| Сценарий | Описание |
| -------- | ----------- |
| Предмет → Качество | К какому уровню качества принадлежит предмет |
| Снаряжение → Персонаж | Какой персонаж может экипировать снаряжение |
| Навык → Класс | К какому классу относится навык |
| Монстр → Таблица дропа | Из какой таблицы дропа выпадает монстр |

## Типы таблиц, на которые можно ссылаться

Следующие типы таблиц могут быть объектами ссылок:

| Тип | Описание |
| ---- | ----------- |
| Таблица dict | Строковый первичный ключ, например ID предмета, ID навыка |
| Таблица list | Целочисленный первичный ключ, например номер монстра, номер предмета |
| Таблица language | Ключ языка, формат \`group_name/language_key\`, например \`&Language\` |

### Специальный формат ссылок на таблицу языков

При ссылке на таблицу языков формат заполняемого значения — \`group_name/language_key\`:

| @dict | Name | Tip Text |
| ----- | ---- | -------- |
| item_id | name | tip |
| string | string | &Language |
| sword_001 | Железный меч | ui/item_tip_001 |

Следующие типы **не могут быть объектами ссылок**:

| Тип | Причина |
| ---- | ------ |
| Таблица enum | Перечисления — это фиксированные варианты, а не таблицы данных |
| Таблица class | Глобальная конфигурация имеет только один экземпляр |
| Базовые типы | i32, string и др. — это типы значений, а не таблицы |

## Ссылки во вложенных типах

Ссылочные типы могут комбинироваться с другими типами для формирования более сложных структур данных.

### Массив ссылок

Когда вам нужно сослаться на несколько целей, можно использовать массив ссылок:

| Формат | Описание |
| ------ | ----------- |
| \`[&T]\` | Массив ссылок, хранит несколько первичных ключей целевой таблицы |

**Пример: Таблица дропа со ссылкой на несколько предметов**

| @dict | Name | Drop Items |
| ----- | ---- | ---------- |
| drop_id | name | items |
| string | string | [&Item] |
| drop_001 | Стартовый набор | sword_001, potion_001 |
| drop_002 | Награда элиты | sword_002, armor_001, gem_001 |

### Другие вложенные комбинации

| Формат | Описание |
| ------ | ----------- |
| \`[&T]\` | Массив ссылок |
| \`Vec<&T>\` | Вектор ссылок |
| \`HashMap<string, &T>\` | Отображение строки в ссылку |

## Валидация ссылок

XCell автоматически проверяет правильность ссылок:

- ✅ Указанные ID должны существовать в целевой таблице
- ✅ Нельзя указать несуществующие ID
- ✅ Циклические ссылки будут обнаружены и сообщены

## Генерация кода

### TypeScript (Cocos, Laya)

\`\`\`typescript
export class Item {
    public itemId: string;
    public name: string;
    public qualityId: string;  // Хранит значение первичного ключа
    public attack: number;
}

export class ItemTable {
    private _items: Map<string, Item>;
    private _qualityTable: QualityTable;

    public getQuality(itemId: string): Quality | undefined {
        const item = this._items.get(itemId);
        if (item) {
            return this._qualityTable.get(item.qualityId);
        }
        return undefined;
    }
}
\`\`\`

### C# (Unity, Godot)

\`\`\`csharp
public class Item
{
    public string ItemId;
    public string Name;
    public string QualityId;  // Хранит значение первичного ключа
    public int Attack;
}

public class ItemTable
{
    private Dictionary<string, Item> _items;
    private QualityTable _qualityTable;

    public Quality GetQuality(string itemId)
    {
        if (_items.TryGetValue(itemId, out var item))
        {
            return _qualityTable.Get(item.QualityId);
        }
        return null;
    }
}
\`\`\`

## Примечания

- Целевая таблица ссылки должна существовать
- Целевая таблица должна быть типа dict, list или language
- Таблицы enum и class не могут быть объектами ссылок
- Базовые типы (i32, string и др.) не могут быть объектами ссылок
- Рекомендуется использовать PascalCase для имён таблиц, например \`&QualityTable\`
`);

write('advanced/type-system.md', `# Система типов

XCell поддерживает множество типов данных. Стандартные типы следуют соглашениям Rust, при этом обеспечивая совместимость с соглашениями C и C++. Типы нечувствительны к регистру.

## Базовые типы

### Целые типы

| Стандартная нотация | Соглашение Unity | Соглашение Unreal | Описание | Диапазон |
| ----------------- | ---------------- | ----------------- | ----------- | ----- |
| \`i8\` | \`char\` | \`int8_t\` | 8-битное знаковое целое | -128 до 127 |
| \`u8\` | \`unsigned char\` | \`uint8_t\` | 8-битное беззнаковое целое | 0 до 255 |
| \`i16\` | \`short\` | \`int16_t\` | 16-битное знаковое целое | -32768 до 32767 |
| \`u16\` | \`unsigned short\` | \`uint16_t\` | 16-битное беззнаковое целое | 0 до 65535 |
| \`i32\` | \`int\` | \`int32_t\` | 32-битное знаковое целое | -2147483648 до 2147483647 |
| \`u32\` | \`unsigned int\` | \`uint32_t\` | 32-битное беззнаковое целое | 0 до 4294967295 |
| \`i64\` | \`long long\` | \`int64_t\` | 64-битное знаковое целое | -9223372036854775808 до 9223372036854775807 |
| \`u64\` | \`unsigned long long\` | \`uint64_t\` | 64-битное беззнаковое целое | 0 до 18446744073709551615 |

<br />

### Типы с плавающей точкой

| Стандартная нотация | Соглашение C | Соглашение C++ | Описание | Точность |
| ----------------- | ------------ | -------------- | ----------- | --------- |
| \`f32\` | \`float\` | \`float\` | 32-битное число с плавающей точкой | Одинарная |
| \`f64\` | \`double\` | \`double\` | 64-битное число с плавающей точкой | Двойная |

### Логический тип

| Стандартная нотация | Соглашение C | Соглашение C++ | Описание | Значения |
| ----------------- | ------------ | -------------- | ----------- | ------ |
| \`bool\` | \`bool\` | \`bool\` | Логическое значение | \`true\` или \`false\` |

### Символьные типы

| Стандартная нотация | Описание |
| ----------------- | ----------- |
| \`utf8\` | Тип символа UTF-8 |
| \`utf16\` | Тип символа UTF-16 |

### Строковые типы

| Стандартная нотация | Описание |
| ----------------- | ----------- |
| \`string\` | Строковый тип |
| \`str\` | Строковый тип (эквивалент string) |

## Составные типы

### Типы массивов и векторов

**Различные соглашения о типах**:

| Стандартная нотация | Соглашение C# | Описание |
| ----------------- | ------------- | ----------- |
| \`[T]\` | \`T[]\` | Динамический массив |
| \`[T; N]\` | \`T[N]\` | Статический массив (фиксированный размер) |
| \`Vec<T>\` | \`List<T>\` | Тип вектора/списка |
| \`vec2<f32>\` | \`Vector2<float>\` | 2D-вектор |
| \`vec3<f32>\` | \`Vector3<float>\` | 3D-вектор |
| \`vec4<f32>\` | \`Vector4<float>\` | 4D-вектор |

**Описание**:

- Динамический массив \`[T]\` и вектор \`Vec<T>\` используются для хранения переменного количества элементов
- Статический массив \`[T; N]\` используется для хранения фиксированного количества элементов, длина определяется при определении
- Специальные векторные типы (vec2/vec3/vec4) часто используются в графическом программировании, Unity и другие движки имеют специфические оптимизации производительности

**Примеры**:

- \`[i32; 5]\` - Статический массив, содержащий 5 элементов i32
- \`[string]\` - Динамический массив строк
- \`Vec<i32>\` - Целочисленный вектор
- \`vec3<f32>\` - 3D-вектор чисел с плавающей точкой

### Тип словаря

**Формат**: \`HashMap<K, V>\` или \`dict<K, V>\`

**Примеры**:

- \`HashMap<string, i32>\` - Отображение строки в целое число
- \`dict<i32, string>\` - Отображение целого числа в строку

### Тип кортежа

**Формат**: \`(T1, T2, ...)\`

**Примеры**:

- \`(i32, string)\` - Кортеж, содержащий целое число и строку
- \`(f32, f32, bool)\` - Кортеж, содержащий два числа с плавающей точкой и одно логическое значение

## Специальные типы

### Тип цвета

| Тип | Описание | Формат |
| ---- | ----------- | ------ |
| \`color\` | Тип цвета | Шестнадцатеричное значение цвета, например \`#FF0000\` |
| \`Color\` | Тип цвета (эквивалент color) | Шестнадцатеричное значение цвета, например \`#FF0000\` |

### Типы времени

| Тип | Описание | Формат |
| ---- | ----------- | ------ |
| \`datetime\` | Тип даты-времени | Формат ISO 8601, например \`2023-12-25T10:30:00\` |
| \`time\` | Тип времени | Например \`10:30:00\` |
| \`date\` | Тип даты | Например \`2023-12-25\` |

<br />

## Уникальный тип и первичный ключ

**Формат уникального типа**:

- \`@T\` - Указывает, что значение поля должно быть уникальным в таблице, т.е. никакие две строки в таблице не могут иметь одинаковое значение этого поля
- \`@@T\` - Указывает, что значение поля должно быть уникальным в таблице и является первичным ключом

**Описание первичного ключа**:

- Первичный ключ — это поле, используемое для уникальной идентификации каждой строки данных в таблице
- Если в таблице не используется \`@@T\` для явной маркировки первичного ключа, первый столбец таблицы автоматически считается первичным ключом
- Значения первичного ключа должны быть уникальными и не могут быть null, используются для установления связей между таблицами

**Примеры**:

- \`@@i32\` - Целочисленное поле первичного ключа
- \`@string\` - Строковое уникальное поле (не первичный ключ)

## Пользовательские типы

### Типы перечислений

Типы перечислений, определённые через таблицы Enumerate, могут использоваться в других таблицах.

**Пример**:

- \`QualityType\` - Ссылается на тип качества, определённый в таблице enum

### Типы структур

Типы структур, определённые через таблицы Class, могут использоваться в других таблицах.

**Пример**:

- \`Position\` - Ссылается на структуру позиции, определённую в таблице Class

## Преобразование типов

XCell поддерживает автоматическое преобразование типов, например:

- Целые числа могут быть автоматически преобразованы в числа с плавающей точкой
- Числа с плавающей точкой могут быть преобразованы в целые числа при необходимости (десятичная часть будет отброшена)
- Числа могут быть преобразованы в строки
- Строки могут быть преобразованы в числа в подходящих случаях

## Валидация типов

XCell выполняет валидацию типов при разборе таблиц, обеспечивая соответствие данных указанным требованиям типов. Если типы не совпадают, ошибки будут сгенерированы во время компиляции.
`);

write('use-cases/index.md', `# Варианты использования

Эта страница содержит примеры использования и лучшие практики XCell в различных сценариях.

## Доступные варианты

- [Интеграция с Unity](unity-integration.md) - Интеграция XCell с игровым движком Unity

## Описание вариантов

### Интеграция с Unity

Подходит для разработчиков, использующих движок Unity для разработки игр. Этот вариант подробно описывает, как бесшовно интегрировать инструмент управления конфигурационными таблицами XCell с проектами Unity, включая:

- Конфигурацию проекта Unity
- Создание и управление конфигурационными таблицами
- Генерацию кода C#
- Загрузку бинарных данных
- Примеры использования во время выполнения
`);

write('use-cases/unity-integration.md', `
# Интеграция с Unity

Это руководство подробно описывает, как бесшовно интегрировать инструмент управления конфигурационными таблицами XCell с проектами Unity.

## Структура проекта

Рекомендуемая структура директорий проекта Unity и рабочей директории XCell:

\`\`\`
MyUnityGame/
├── UnityProject/          # Корневая директория проекта Unity
│   ├── Assets/
│   │   ├── Scripts/
│   │   │   └── DataTable/
│   │   │       └── Generated/    # Автоматически сгенерированный код C#
│   │   └── Tables/
│   │       └── Generated/         # Автоматически сгенерированные бинарные данные
│   └── ProjectSettings/
└── XCellWork/             # Рабочая директория XCell
    ├── xcell.exe
    ├── ProjectConfig.toml
    └── Tables/
        ├── Hero.xlsx
        ├── Item.xlsx
        └── Skill.xlsx
\`\`\`

## Настройка ProjectConfig.toml

Создайте \`ProjectConfig.toml\` в рабочей директории XCell со следующей конфигурацией:

\`\`\`toml
version = "1.0.0"

exclude = ""
include = "*.xlsx"

line.field = 1
line.type = 2
line.comment = 3
line.data = 4

[type.bool]
accept = ["true", "√", "是"]
reject = ["false", "x", "否"]

[type.string]

[unity]
enable = true
project = "../UnityProject"
output = "Assets/Scripts/DataTable/Generated"
namespace = "MyGame.DataTable"
manager = "DataTableManager"
suffix_table = "Table"
suffix_element = "Data"
support_clone = true
legacy_using = false
legacy_null_null = false

[unity.binary]
enable = true
output = "Assets/Tables/Generated"

[unity.xlua]
enable = false

[unity.xml]
enable = false
output = "Assets/Tables/Readable"

[unity.json]
enable = false
output = "Assets/Tables/Readable"

[unity.protobuf]
enable = false
\`\`\`

### Описание конфигурации

| Параметр конфигурации | Описание |
|--------------------|-------------|
| \`unity.project\` | Относительный путь к проекту Unity |
| \`unity.output\` | Путь генерации кода C# |
| \`unity.namespace\` | Пространство имён сгенерированного кода |
| \`unity.manager\` | Имя класса менеджера данных |
| \`unity.suffix_table\` | Суффикс имени класса таблицы |
| \`unity.suffix_element\` | Суффикс имени класса элемента данных |
| \`unity.binary.output\` | Путь вывода бинарных данных |

## Создание примеров конфигурационных таблиц

### 1. Таблица конфигурации героев (Hero.xlsx)

| id | name | name_key | hp | attack | defense | speed | rarity | skill_ids |
|----|------|----------|----|--------|---------|-------|--------|-----------|
| int | string | string | int | int | int | float | int | int[] |
| ID героя | Имя героя | Ключ локализации имени | Очки здоровья | Сила атаки | Защита | Скорость | Редкость | Список ID навыков |
| 1001 | Рыцарь | hero_knight | 1500 | 120 | 80 | 1.2 | 2 | [101, 102] |
| 1002 | Маг | hero_mage | 900 | 180 | 40 | 1.5 | 2 | [201, 202] |
| 1003 | Лучник | hero_archer | 1000 | 150 | 50 | 1.8 | 2 | [301, 302] |

### 2. Таблица конфигурации предметов (Item.xlsx)

| id | name | type | price | stackable | max_stack | description |
|----|------|------|-------|-----------|-----------|-------------|
| int | string | int | int | bool | int | string |
| ID предмета | Имя предмета | Тип предмета | Цена | Складируется | Макс. стопка | Описание |
| 1 | Зелье здоровья | 1 | 50 | true | 99 | Восстанавливает 500 HP |
| 2 | Зелье маны | 1 | 60 | true | 99 | Восстанавливает 300 MP |
| 3 | Железный меч | 2 | 500 | false | 1 | Базовое оружие, Атака +50 |

### 3. Таблица конфигурации навыков (Skill.xlsx)

| id | name | damage | cooldown | mp_cost | target_type |
|----|------|--------|----------|---------|-------------|
| int | string | int | float | int | int |
| ID навыка | Имя навыка | Урон | Перезарядка | Стоимость MP | Тип цели |
| 101 | Удар | 100 | 2.0 | 0 | 1 |
| 102 | Удар щитом | 80 | 3.0 | 10 | 1 |
| 201 | Огненный шар | 200 | 3.5 | 30 | 2 |

## Запуск XCell для генерации кода

Откройте командную строку в рабочей директории XCell и выполните:

\`\`\`bash
xcell.exe
\`\`\`

После завершения генерации вы увидите следующие файлы в проекте Unity:

\`\`\`
UnityProject/
├── Assets/
│   ├── Scripts/DataTable/Generated/
│   │   ├── HeroTable.cs
│   │   ├── ItemTable.cs
│   │   ├── SkillTable.cs
│   │   └── DataTableManager.cs
│   └── Tables/Generated/
│       ├── HeroTable.bytes
│       ├── ItemTable.bytes
│       └── SkillTable.bytes
\`\`\`

## Создание загрузчика данных

Создайте скрипт \`DataTableLoader.cs\` в проекте Unity:

\`\`\`csharp
using System;
using System.IO;
using UnityEngine;
using MyGame.DataTable;

public class DataTableLoader : MonoBehaviour
{
    private static DataTableLoader _instance;
    public static DataTableLoader Instance => _instance;

    public HeroTable HeroTable { get; private set; }
    public ItemTable ItemTable { get; private set; }
    public SkillTable SkillTable { get; private set; }

    private void Awake()
    {
        if (_instance != null && _instance != this)
        {
            Destroy(gameObject);
            return;
        }

        _instance = this;
        DontDestroyOnLoad(gameObject);
        LoadAllTables();
    }

    private void LoadAllTables()
    {
        HeroTable = LoadTable<HeroTable>("HeroTable");
        ItemTable = LoadTable<ItemTable>("ItemTable");
        SkillTable = LoadTable<SkillTable>("SkillTable");
    }

    private T LoadTable<T>(string tableName) where T : class, new()
    {
        TextAsset textAsset = Resources.Load<TextAsset>($"Tables/{tableName}");
        if (textAsset == null)
        {
            Debug.LogError($"Не удалось загрузить таблицу: {tableName}");
            return null;
        }

        using (MemoryStream stream = new MemoryStream(textAsset.bytes))
        using (BinaryReader reader = new BinaryReader(stream))
        {
            T table = new T();
            var method = typeof(T).GetMethod("Read");
            if (method != null)
            {
                method.Invoke(table, new object[] { reader });
            }
            return table;
        }
    }
}
\`\`\`

## Использование конфигурационных таблиц в игре

### Пример 1: Получение данных героя

\`\`\`csharp
public class Hero : MonoBehaviour
{
    public int heroId;
    private HeroData _heroData;

    private void Start()
    {
        _heroData = DataTableLoader.Instance.HeroTable.GetElement(heroId);
        if (_heroData == null)
        {
            Debug.LogError($"Данные героя не найдены: {heroId}");
            return;
        }

        InitializeHero();
    }

    private void InitializeHero()
    {
        Debug.Log($"Загрузка героя: {_heroData.name}");
        Debug.Log($"Очки здоровья: {_heroData.hp}");
        Debug.Log($"Сила атаки: {_heroData.attack}");
    }
}
\`\`\`

### Пример 2: Интеграция системы предметов

\`\`\`csharp
public class InventoryManager : MonoBehaviour
{
    public void UseItem(int itemId)
    {
        ItemData itemData = DataTableLoader.Instance.ItemTable.GetElement(itemId);
        if (itemData == null)
        {
            Debug.LogError($"Предмет не найден: {itemId}");
            return;
        }

        switch (itemData.type)
        {
            case 1:
                UseConsumable(itemData);
                break;
            case 2:
                EquipWeapon(itemData);
                break;
        }
    }

    private void UseConsumable(ItemData item)
    {
        Debug.Log($"Использование предмета: {item.name}");
    }

    private void EquipWeapon(ItemData item)
    {
        Debug.Log($"Экипировка оружия: {item.name}");
    }
}
\`\`\`

### Пример 3: Система навыков

\`\`\`csharp
public class SkillController : MonoBehaviour
{
    public void CastSkill(int skillId, GameObject caster, GameObject target)
    {
        SkillData skillData = DataTableLoader.Instance.SkillTable.GetElement(skillId);
        if (skillData == null)
        {
            Debug.LogError($"Навык не найден: {skillId}");
            return;
        }

        Debug.Log($"Применение навыка: {skillData.name}, Урон: {skillData.damage}");
    }
}
\`\`\`

## Рекомендации по рабочему процессу

### Фаза разработки

1. Редактируйте конфигурационные таблицы в Excel
2. Запустите \`xcell.exe\` для генерации кода и данных
3. Обновите проект Unity
4. Тестируйте в Unity

### Режим наблюдения

Во время разработки вы можете использовать режим наблюдения для автоматического обнаружения изменений файлов:

\`\`\`bash
xcell.exe --watch
\`\`\`

Таким образом, при изменении таблиц Excel XCell будет автоматически перегенерировать код и файлы данных.

## Лучшие практики

1. **Управление версиями**: Включите таблицы Excel и \`ProjectConfig.toml\` в управление версиями, исключите сгенерированный код и бинарные файлы
2. **Организация папок**: Организуйте файлы таблиц Excel по функциональным модулям
3. **Соглашения об именовании**: Используйте единые соглашения об именовании, такие как PascalCase или snake_case
4. **Валидация данных**: Регулярно используйте \`xcell.exe check\` для проверки корректности данных конфигурационных таблиц
5. **Сопровождение документации**: Добавляйте документацию для сложных конфигурационных таблиц
`);

write('maintainer/architecture/index.md', `# Документ по архитектурному дизайну XCell

## 1. Общий обзор архитектуры проекта

XCell — это инструмент управления конфигурационными таблицами, написанный на Rust, использующий модульный дизайн с чётким разделением ответственности модулей и слабой связностью. Проект разделён на следующие основные части:

- **Бэкенд-модули**: Расположены в директории \`backends/\`, содержат основную бизнес-логику
  - \`xcell\` - Инструмент командной строки и основная точка входа
  - \`xcell-analyzer\` - Управление рабочим пространством и анализ таблиц
  - \`xcell-generator\` - Генератор кода
  - \`xcell-provider\` - Абстракция чтения таблиц
  - \`xcell-core\` - Система типов и базовая функциональность
  - \`xcell-config\` - Управление конфигурацией
  - \`xcell-macros\` - Определения макросов
  - \`xcell-parser\` - Парсер типов
  - \`xcell-plugin\` - Система плагинов
  - \`xcell-wasi\` - Поддержка WebAssembly

- **Фронтенд-модули**: Расположены в директории \`frontends/\`, содержат пользовательские интерфейсы
  - \`homepage\` - Официальный сайт проекта
  - \`xcell\` - Фронтенд SDK
  - \`xcell-desktop\` - Десктопное приложение
  - \`xcell-h5\` - Веб-приложение

- **Документация**: Расположена в директории \`documentation/\`, содержит документацию проекта

- **Примеры**: Расположены в директории \`examples/\`, содержат примеры использования

### Технологический стек

- **Бэкенд**: Rust
- **Фронтенд**: Vue.js, TypeScript, Tauri
- **Чтение таблиц**: calamine (Excel), csv (CSV/TSV)
- **Шаблонизатор**: dejavu
- **Асинхронная среда выполнения**: tokio
- **Обработка ошибок**: anyhow
- **Логирование**: tracing

## 2. Разделение модулей и описание ответственности

### 2.1 xcell - Инструмент командной строки

**Ответственность**:
- Предоставление интерфейса командной строки
- Разбор аргументов командной строки
- Координация всего рабочего процесса
- Вызов других бэкенд-модулей для выполнения задач

**Основные файлы**:
- \`backends/xcell/src/main.rs\` - Точка входа программы
- \`backends/xcell/src/workspace.rs\` - Управление рабочим пространством
- \`backends/xcell/src/commands/toml.rs\` - Обработка конфигурации TOML

**Основные функции**:
- Генерация кода и файлов данных
- Проверка конфигурации
- Очистка вывода
- Режим наблюдения за файлами

### 2.2 xcell-analyzer - Управление рабочим пространством и анализ таблиц

**Ответственность**:
- Управление рабочим пространством и конфигурацией
- Сканирование и идентификация файлов таблиц
- Разбор данных таблиц
- Идентификация типов таблиц
- Обработка данных таблиц
- Привязка определений перечислений

**Основные файлы**:
- \`backends/xcell-analyzer/src/lib.rs\` - Экспорт модуля
- \`backends/xcell-analyzer/src/config/mod.rs\` - Менеджер рабочего пространства
- \`backends/xcell-analyzer/src/x_table/mod.rs\` - Структуры данных таблиц

**Основные компоненты**:
- \`WorkspaceManager\` - Менеджер рабочего пространства, отвечает за координацию всего рабочего процесса
- \`XClassTable\` - Тип таблицы Class
- \`XDictTable\` - Тип таблицы Dictionary
- \`XEnumerateTable\` - Тип таблицы Enum
- \`XLanguageTable\` - Тип таблицы Language
- \`DefineManager\` - Менеджер определений перечислений
- \`LanguageManager\` - Менеджер таблиц языков

### 2.3 xcell-generator - Генератор кода

**Ответственность**:
- Генерация кода и файлов данных в различных форматах
- Поддержка нескольких целевых платформ
- Предоставление подключаемой архитектуры генерации кода

**Основные файлы**:
- \`backends/xcell-generator/src/lib.rs\` - Экспорт модуля
- \`backends/xcell-generator/src/codegen/mod.rs\` - Интерфейс генератора кода
- \`backends/xcell-generator/src/config.rs\` - Конфигурация генератора

**Поддерживаемые генераторы кода**:
- \`json\` - Генерация данных JSON ✅
- \`binary\` - Генерация бинарных данных ✅
- \`cocos\` - Генерация кода для платформы Cocos ✅
- \`typescript\` - Генерация кода TypeScript ✅
- \`dejavu\` - Генерация кода через шаблонизатор ✅
- \`unity\` - Генерация кода для платформы Unity ⚠️ (в настоящее время отключена)
- \`xlua\` - Генерация скриптов XLua
- \`sql\` - Генерация кода для базы данных SQL
- \`xml\` - Генерация данных XML

### 2.4 xcell-provider - Абстракция чтения таблиц

**Ответственность**:
- Предоставление унифицированного интерфейса чтения таблиц
- Поддержка нескольких форматов таблиц (Excel, CSV, TSV)
- Абстрагирование различий между форматами таблиц
- Предоставление разбора заголовков таблиц

**Основные файлы**:
- \`backends/xcell-provider/src/lib.rs\` - Экспорт модуля
- \`backends/xcell-provider/src/table/mod.rs\` - Интерфейс чтения таблиц
- \`backends/xcell-provider/src/standard/mod.rs\` - Реализация стандартного потока

**Основные компоненты**:
- \`TableReader\` - Типаж чтения таблиц
- \`ExcelTable\` - Реализация чтения таблиц Excel
- \`CsvTable\` - Реализация чтения таблиц CSV
- \`TsvTable\` - Реализация чтения таблиц TSV
- \`FileFormatDetector\` - Детектор формата файлов
- \`load_table\` - Унифицированная функция загрузки таблиц

### 2.5 xcell-core - Система типов и базовая функциональность

**Ответственность**:
- Определение всех типов данных
- Предоставление преобразования и разбора типов
- Поддержка маппинга типов для различных платформ
- Предоставление обработки и преобразования значений
- Предоставление интерфейсов чтения/записи с учётом порядка байтов

**Основные файлы**:
- \`backends/xcell-core/src/lib.rs\` - Экспорт модуля
- \`backends/xcell-core/src/typing/mod.rs\` - Определения типов
- \`backends/xcell-core/src/value/mod.rs\` - Обработка значений

**Поддерживаемые типы**:
- Целые типы (Integer)
- Десятичные типы (Decimal)
- Логические типы (Boolean)
- Строковые типы (String)
- Типы массивов (Array)
- Векторные типы (Vector)
- Языковые типы (Language)
- Типы перечислений (Enumerate)
- Типы цветов (Color)
- Типы времени (Time)

### 2.6 xcell-config - Управление конфигурацией

**Ответственность**:
- Определение структуры конфигурации проекта
- Предоставление разбора и валидации конфигурации
- Поддержка параметров конфигурации для различных платформ

**Основные файлы**:
- \`backends/xcell-config/src/lib.rs\` - Экспорт модуля
- \`backends/xcell-config/src/project/mod.rs\` - Конфигурация проекта
- \`backends/xcell-config/src/cocos/mod.rs\` - Конфигурация платформы Cocos
- \`backends/xcell-config/src/unity/mod.rs\` - Конфигурация платформы Unity

**Основные компоненты**:
- \`ProjectConfig\` - Конфигурация проекта
- \`CocosCodegen\` - Конфигурация генерации кода Cocos
- \`UnityCodegen\` - Конфигурация генерации кода Unity
- \`MergeRules\` - Правила слияния таблиц

### 2.7 xcell-parser - Парсер типов

**Ответственность**:
- Разбор выражений типов
- Разбор определений полей
- Разбор метаданных

**Основные файлы**:
- \`backends/xcell-parser/src/lib.rs\` - Экспорт модуля
- \`backends/xcell-parser/src/lexer.rs\` - Лексер
- \`backends/xcell-parser/src/parser.rs\` - Парсер
- \`backends/xcell-parser/src/ast.rs\` - Абстрактное синтаксическое дерево

## 3. Описание потока данных

### 3.1 Общий процесс

Полный процесс от чтения файла таблицы до экспорта кода:

\`\`\`
Файл таблицы (Excel/CSV/TSV) → Чтение/Разбор → Распознавание таблицы → Обработка данных → Генерация кода → Выходные файлы
\`\`\`

### 3.2 Подробные шаги

#### Шаг 1: Инициализация рабочего пространства

1. Разбор аргументов командной строки или файла конфигурации
2. Создание экземпляра \`WorkspaceManager\`
3. Загрузка конфигурации проекта (\`ProjectConfig\`)

#### Шаг 2: Сканирование файлов

1. Сканирование рабочей директории
2. Использование \`WalkDir\` для обхода директории
3. Фильтрация файлов по настроенному шаблону \`include\`

#### Шаг 3: Чтение файлов таблиц

1. Использование функции \`load_table()\` для чтения файлов таблиц (автоопределение формата)
2. Разбор заголовка таблицы (\`XCellHeader\`)
3. Чтение всех строк данных

#### Шаг 4: Идентификация типа таблицы

Попытка идентификации следующих типов таблиц по порядку:

1. \`XListTable\` - Таблица List
2. \`XDictTable\` - Таблица Dictionary
3. \`XEnumerateTable\` - Таблица Enum
4. \`XClassTable\` - Таблица Class
5. \`XLanguageTable\` - Таблица Language
6. \`XLanguageID\` - Таблица ID языков

#### Шаг 5: Обработка данных таблицы

Выполнение соответствующих операций в зависимости от типа таблицы:

- Для таблиц enum: Добавление в \`DefineManager\`
- Для таблиц языков: Добавление в \`LanguageManager\`
- Для других таблиц: Выполнение валидации данных и сохранение

#### Шаг 6: Привязка перечислений

Вызов метода \`link_enumerate()\` для привязки определений перечислений к соответствующим полям данных

#### Шаг 7: Генерация кода

1. Создание экземпляра \`Generator\`
2. Настройка включённых генераторов кода
3. Итерация по всем включённым выводам
4. Вызов соответствующего генератора кода для каждого вывода
5. Генерация кода и файлов данных в соответствующих форматах

#### Шаг 8: Наблюдение за файлами (опционально)

Если наблюдение за файлами включено:
1. Запуск наблюдателя файлов
2. Ожидание изменений файлов
3. Автоматическая повторная обработка изменённых файлов

## 4. Справочник расположения основного кода

### Управление рабочим пространством
- \`WorkspaceManager\` - \`backends/xcell-analyzer/src/config/mod.rs\`
- \`WorkspaceManager::new()\` - Создание менеджера рабочего пространства
- \`WorkspaceManager::classes()\` - Получение данных таблиц Class
- \`WorkspaceManager::lists()\` - Получение данных таблиц List
- \`WorkspaceManager::dicts()\` - Получение данных таблиц Dict
- \`WorkspaceManager::enumerates()\` - Получение данных таблиц Enum

### Чтение таблиц
- \`load_table()\` - \`backends/xcell-provider/src/table/mod.rs\` - Унифицированная функция загрузки таблиц
- \`TableReader\` - \`backends/xcell-provider/src/table/mod.rs\` - Типаж чтения таблиц
- \`XCellHeader\` - \`backends/xcell-provider/src/table/mod.rs\` - Заголовок таблицы

### Типы таблиц
- \`XClassTable\` - \`backends/xcell-analyzer/src/x_table/class/mod.rs\` - Тип таблицы Class
- \`XDictTable\` - \`backends/xcell-analyzer/src/x_table/dictionary/mod.rs\` - Тип таблицы Dictionary
- \`XEnumerateTable\` - \`backends/xcell-analyzer/src/x_table/enumerate/mod.rs\` - Тип таблицы Enum
- \`XLanguageTable\` - \`backends/xcell-analyzer/src/x_table/language/mod.rs\` - Тип таблицы Language

### Генерация кода
- \`Generator\` - \`backends/xcell-generator/src/lib.rs\` - Основной вход генератора
- \`Codegen\` - \`backends/xcell-generator/src/codegen/mod.rs\` - Типаж генератора кода
- \`CocosCodegen\` - \`backends/xcell-generator/src/codegen/cocos/mod.rs\` - Генерация кода Cocos
- \`UnityCodegen\` - \`backends/xcell-generator/src/codegen/unity/mod.rs\` - Генерация кода Unity (в настоящее время отключена)
- \`JsonCodegen\` - \`backends/xcell-generator/src/codegen/json/mod.rs\` - Генерация данных JSON

### Система типов
- \`TypeDescription\` - \`backends/xcell-core/src/typing/mod.rs\` - Описание типа
- \`XCellValue\` - \`backends/xcell-core/src/value/mod.rs\` - Значение ячейки
- \`CSharpReader\`/\`CSharpWriter\` - \`backends/xcell-core/src/codegen/csharp_ffi/mod.rs\` - Маппинг типов C#

### Управление конфигурацией
- \`ProjectConfig\` - \`backends/xcell-config/src/project/mod.rs\` - Конфигурация проекта
- \`CocosCodegen\` - \`backends/xcell-config/src/cocos/mod.rs\` - Конфигурация генерации кода Cocos
- \`UnityCodegen\` - \`backends/xcell-config/src/unity/mod.rs\` - Конфигурация генерации кода Unity

## 5. Дизайн изоляции абстракций

### 5.1 Основные уровни абстракции

XCell использует многоуровневый дизайн абстракций, обеспечивая чёткое разделение ответственности модулей и предотвращая утечку абстракций:

1. **Уровень чтения таблиц** (\`xcell-provider\`):
   - Предоставляет унифицированный типаж \`TableReader\`
   - Абстрагирует различия между форматами таблиц (Excel, CSV, TSV)
   - Верхние модули не должны знать о конкретных форматах таблиц

2. **Уровень анализа таблиц** (\`xcell-analyzer\`):
   - Читает данные таблиц на основе \`TableReader\`
   - Идентифицирует типы таблиц и выполняет соответствующую обработку
   - Предоставляет \`WorkspaceManager\` для унифицированного управления всеми данными таблиц

3. **Уровень генерации кода** (\`xcell-generator\`):
   - Получает данные таблиц на основе \`WorkspaceManager\`
   - Не взаимодействует напрямую с файлами таблиц
   - Поддерживает несколько генераторов кода через типаж \`Codegen\`

4. **Уровень системы типов** (\`xcell-core\`):
   - Определяет унифицированные типы данных
   - Предоставляет преобразование и разбор типов
   - Поддерживает маппинг типов для нескольких платформ

### 5.2 Принципы изоляции абстракций

- **Единая ответственность**: Каждый модуль отвечает только за одну конкретную функцию
- **Инверсия зависимостей**: Высокоуровневые модули зависят от абстракций, а не от конкретных реализаций
- **Разделение интерфейсов**: Использование типажей для определения минимальных интерфейсов
- **Принцип подстановки Барбары Лисков**: Реализации могут быть заменены их подтипами
- **Принцип открытости/закрытости**: Открыты для расширения, закрыты для модификации

## 6. Руководство по расширению

### Добавление нового формата таблиц

1. Создайте новую реализацию чтения таблиц в \`backends/xcell-provider/src/table/\`
2. Реализуйте типаж \`TableReader\`
3. Добавьте логику обнаружения формата в \`FileFormatDetector\`
4. Добавьте поддержку нового формата в функцию \`load_table\`

### Добавление нового типа данных

1. Создайте новый модуль в \`backends/xcell-core/src/\`
2. Реализуйте логику разбора и преобразования типа
3. Экспортируйте в \`backends/xcell-core/src/lib.rs\`
4. Добавьте поддержку маппинга типов для соответствующих платформ

### Добавление нового генератора кода

1. Создайте новый модуль в \`backends/xcell-generator/src/codegen/\`
2. Реализуйте типаж \`Codegen\`
3. Зарегистрируйте новый генератор в \`Generator::new()\`
4. Добавьте соответствующие параметры конфигурации

### Добавление поддержки новой платформы

1. Создайте новый модуль конфигурации платформы в \`backends/xcell-config/src/\`
2. Создайте новый генератор кода платформы в \`backends/xcell-generator/src/codegen/\`
3. Реализуйте специфичную для платформы логику генерации кода
4. Обновите документацию и примеры
`);

write('maintainer/targets/index.md', `# Целевые платформы

XCell поддерживает несколько целевых платформ, включая фронтенд и бэкенд. В этой главе подробно описаны методы интеграции и маппинг типов для каждой платформы.

## Фронтенд-платформы

Фронтенд-платформы в основном ориентированы на игровые движки и фронтенд-фреймворки, предоставляя функциональность генерации кода и загрузки данных.

### Игровые движки

- [Unity](unity.md) - Интеграция с движком Unity ⚠️ (в настоящее время отключена)
- [Cocos](cocos.md) - Интеграция с движком Cocos ✅
- [Unreal Engine](unreal.md) - Интеграция с Unreal Engine
- [Godot](godot.md) - Интеграция с движком Godot
- [XLua](xlua.md) - Интеграция со скриптами XLua

### Фронтенд-фреймворки

- [React](react.md) - Интеграция с фреймворком React
- [Vue](vue.md) - Интеграция с фреймворком Vue
- [TypeScript/JavaScript](typescript.md) - Интеграция с TypeScript/JavaScript ✅

## Бэкенд-платформы

Бэкенд-платформы в основном ориентированы на серверную часть и хранение данных, предоставляя персистентность данных и серверную интеграцию.

### Форматы данных

- [JSON](json.md) - Формат данных JSON ✅
- [SQL](sql.md) - Интеграция с базой данных SQL

## Описание статуса генераторов кода

| Генератор | Статус | Описание |
| --------- | ------ | ----------- |
| \`json\` | ✅ Доступен | Генерация данных JSON |
| \`binary\` | ✅ Доступен | Генерация бинарных данных |
| \`cocos\` | ✅ Доступен | Генерация кода для платформы Cocos |
| \`typescript\` | ✅ Доступен | Генерация кода TypeScript |
| \`dejavu\` | ✅ Доступен | Генерация кода через шаблонизатор |
| \`unity\` | ⚠️ Отключён | Генерация кода для платформы Unity (в процессе рефакторинга) |
| \`xlua\` | В разработке | Генерация скриптов XLua |
| \`sql\` | В разработке | Генерация кода для базы данных SQL |
| \`xml\` | В разработке | Генерация данных XML |

## Кроссплатформенная поддержка

XCell разработан как кроссплатформенный инструмент, поддерживающий совместное использование конфигурационных данных между различными платформами, обеспечивая согласованность данных и эффективность разработки.

### Кроссплатформенный обмен данными

- Унифицированная модель данных
- Кроссплатформенный маппинг типов
- Стандартизированный формат конфигурации

### Лучшие практики

- Выбирайте подходящий формат данных в зависимости от целевой платформы
- Используйте правила слияния для управления кроссплатформенными данными
- Регулярно синхронизируйте структуры данных между платформами
`);

write('maintainer/targets/cocos.md', `# Интеграция с Cocos

> ✅ **Доступно**: Генератор кода Cocos в настоящее время доступен, поддерживает генерацию кода TypeScript и файлов данных JSON.

XCell обеспечивает глубокую интеграцию с движком Cocos, поддерживая генерацию кода TypeScript, файлов данных JSON и других форматов.

## Параметры конфигурации

В файле \`ProjectSettings.toml\` конфигурация интеграции Cocos расположена в секции \`[cocos]\`:

\`\`\`toml
[cocos]
enable = true
project = "../"                    # Директория проекта Cocos
output = "assets/scripts/DataTable/Generated"  # Директория вывода кода TypeScript
manager_name = "DataTableManager"  # Имя класса менеджера
suffix_table = "Table"             # Суффикс класса таблицы
instance_name = "dataTable"        # Имя экземпляра
table_data_path = "assets/tables"  # Префикс пути к данным таблиц

# Конфигурация хранения JSON
[cocos.storage.Json]
enable = true
output = "assets/tables/Generated" # Директория вывода данных JSON

# Конфигурация хранения для среды разработки (опционально)
[cocos.storage_debug.Json]
enable = true
output = "assets/tables/Debug"
\`\`\`

## Структура сгенерированного кода

### Структура класса таблицы

Каждая конфигурационная таблица генерирует соответствующие классы TypeScript, включая:
- Класс данных таблицы (Table)
- Класс данных элемента (Element)
- Класс менеджера (Manager)

### Пример структуры сгенерированного кода:

\`\`\`typescript
namespace DataTable.Generated {
    export class BuffTable {
        private data: Map<number, BuffElement> = new Map();

        public get(id: number): BuffElement {
            return this.data.get(id);
        }

        public tryGet(id: number): BuffElement | undefined {
            return this.data.get(id);
        }

        public load(data: any[]): void {
            for (const item of data) {
                const element = new BuffElement();
                element.id = item.id;
                element.name = item.name;
                element.value = item.value;
                this.data.set(item.id, element);
            }
        }
    }

    export class BuffElement {
        public id: number = 0;
        public name: string = "";
        public value: number = 0;
        // ... другие поля
    }

    export class DataTableManager {
        public buffTable: BuffTable = new BuffTable();

        public async loadAll(): Promise<void> {
            // Загрузка всех данных таблиц
        }

        public unloadAll(): void {
            // Выгрузка всех данных таблиц
        }
    }
}
\`\`\`

## Загрузка данных

### Загрузка данных JSON:

\`\`\`typescript
import { DataTableManager } from "../scripts/DataTable/Generated/Manager";

const manager = new DataTableManager();
await manager.loadAll();

// Использование данных
const buff = manager.buffTable.get(1);
console.log(buff?.name);
\`\`\`

### Поддерживаемые функции:
- Асинхронная загрузка
- Инкрементальная загрузка
- Управление памятью
- Поддержка горячих обновлений

## Маппинг типов

Маппинг типов XCell в типы TypeScript:

| Тип XCell | Тип TypeScript |
| ---------- | --------------- |
| bool | boolean |
| i8 | number |
| i16 | number |
| i32 | number |
| i64 | number |
| u8 | number |
| u16 | number |
| u32 | number |
| u64 | number |
| f32 | number |
| f64 | number |
| string | string |
| color | string (шестнадцатеричный) |
| vec2 | { x: number, y: number } |
| vec3 | { x: number, y: number, z: number } |
| vec4 | { x: number, y: number, z: number, w: number } |

## Описание полей конфигурации

| Поле | Тип | Значение по умолчанию | Описание |
| ----- | ---- | ------------- | ----------- |
| \`enable\` | \`bool\` | \`false\` | Включить ли генерацию кода Cocos |
| \`project\` | \`string\` | \`"../"\` | Директория проекта Cocos |
| \`output\` | \`string\` | \`""\` | Директория вывода кода TypeScript |
| \`manager_name\` | \`string\` | \`""\` | Имя класса менеджера |
| \`suffix_table\` | \`string\` | \`""\` | Суффикс класса таблицы |
| \`instance_name\` | \`string\` | \`""\` | Имя экземпляра |
| \`table_data_path\` | \`string\` | \`""\` | Префикс пути к данным таблицы |
| \`storage\` | \`CocosStorage\` | \`Json\` | Конфигурация формата хранения |
| \`storage_debug\` | \`Option<CocosStorage>\` | \`None\` | Конфигурация хранения для среды разработки |

## Оптимизация производительности

### Оптимизация обработки больших таблиц

1. **Разумное разделение больших таблиц**
   - Разделяйте большие таблицы по функциям или модулям
   - Используйте правила слияния по строкам для объединения при сборке
   - Поддерживайте удобство сопровождения во время разработки и производительность во время выполнения

2. **Использование подходящего формата данных**
   - Используйте формат JSON в среде разработки для отладки
   - Рассмотрите использование бинарного формата в производственной среде для повышения скорости загрузки

### Оптимизация инкрементальных обновлений

1. **Режим наблюдения**
   Используйте режим наблюдения:
   \`\`\`bash
   xcell.exe --watch
   \`\`\`
   Особенности режима наблюдения:
   - Регенерация только изменённых файлов
   - Значительное повышение эффективности разработки
   - Поддержка предварительного просмотра в реальном времени

2. **Разумная конфигурация наблюдения**
   Настройте разумные шаблоны include/exclude для уменьшения количества наблюдаемых файлов.

### Оптимизация памяти

1. **Загрузка только необходимых таблиц**
   \`\`\`typescript
   // Загрузка только определённых таблиц
   await manager.buffTable.load();
   await manager.itemTable.load();
   \`\`\`

2. **Своевременная выгрузка**
   \`\`\`typescript
   // Выгрузка ненужных таблиц
   manager.buffTable.unload();
   \`\`\`

## Частые проблемы

### Ошибки компиляции сгенерированного кода

- Проверьте правильность пути к проекту Cocos
- Проверьте соответствие пространства имён структуре проекта
- Убедитесь, что все зависимости правильно установлены

### Сбой загрузки данных

- Проверьте, сгенерированы ли JSON файлы
- Проверьте правильность путей к файлам
- Убедитесь, что структура таблицы соответствует типам данных

## Лучшие практики

1. **Используйте формат мета-таблиц**: Для сложных конфигурационных таблиц рекомендуется формат мета-таблиц, поддерживающий более богатые определения метаданных
2. **Разумно используйте правила слияния**: Для больших проектов используйте правила слияния для управления сложными таблицами
3. **Оптимизируйте структуры данных**: Выбирайте подходящие типы данных и структуры в зависимости от реальных вариантов использования
4. **Регулярная очистка**: Регулярно очищайте ненужные конфигурационные таблицы и данные для поддержания порядка в проекте
`);

write('maintainer/targets/godot.md', `# Интеграция с Godot

XCell обеспечивает глубокую интеграцию с движком Godot, поддерживая генерацию кода GDScript, файлов данных JSON и других форматов.

## Параметры конфигурации

В файле \`XCell.toml\` конфигурация интеграции Godot расположена в секции \`[godot]\`:

\`\`\`toml
[godot]
enable = true
project = "../"
output = "scripts/DataTable/Generated"
namespace = "DataTable"
manager = "DataTableManager"
suffix_table = "Table"
suffix_element = "Element"

[godot.json]
enable = true
output = "res://tables/Generated"

[godot.binary]
enable = false
output = "res://tables/Binary"
\`\`\`

## Структура сгенерированного кода

### Структура класса таблицы

Каждая конфигурационная таблица генерирует соответствующие классы GDScript, включая:
- Класс данных таблицы (Table)
- Класс данных элемента (Element)
- Класс менеджера (Manager)

### Пример структуры сгенерированного кода:

\`\`\`gdscript
# BuffTable.gd
class_name BuffTable

var data = {}

func get(id):
    return data.get(id)

func try_get(id):
    return data.get(id, null)

func load(data_array):
    for item in data_array:
        var element = BuffElement.new()
        element.id = item.id
        element.name = item.name
        element.value = item.value
        data[item.id] = element

# BuffElement.gd
class_name BuffElement

var id = 0
var name = ""
var value = 0
# ... другие поля

# DataTableManager.gd
class_name DataTableManager

var buff_table = BuffTable.new()

func load_all():
    # Загрузка всех данных таблиц
    pass

func unload_all():
    # Выгрузка всех данных таблиц
    pass
\`\`\`

## Загрузка данных

### Загрузка данных JSON:

\`\`\`gdscript
var manager = DataTableManager.new()
manager.load_all()

# Использование данных
var buff = manager.buff_table.get(1)
if buff:
    print(buff.name)
\`\`\`

### Поддерживаемые функции:
- Асинхронная загрузка
- Инкрементальная загрузка
- Управление памятью
- Поддержка горячих обновлений

## Маппинг типов

Маппинг типов XCell в типы GDScript:

| Тип XCell | Тип GDScript |
| ---------- | ------------- |
| bool | bool |
| i8 | int |
| i16 | int |
| i32 | int |
| i64 | int |
| u8 | int |
| u16 | int |
| u32 | int |
| u64 | int |
| f32 | float |
| f64 | float |
| string | String |
| color | Color |
| vec2 | Vector2 |
| vec3 | Vector3 |
| vec4 | Vector4 |

## Оптимизация производительности

### Оптимизация обработки больших таблиц

1. **Разумное разделение больших таблиц**
   - Разделяйте большие таблицы по функциям или модулям
   - Используйте правила слияния по строкам для объединения при сборке
   - Поддерживайте удобство сопровождения во время разработки и производительность во время выполнения

2. **Использование подходящего формата данных**
   - Используйте формат JSON в среде разработки для отладки
   - Рассмотрите использование бинарного формата в производственной среде для повышения скорости загрузки

### Оптимизация инкрементальных обновлений

1. **Режим наблюдения**
   Используйте режим наблюдения:
   \`\`\`bash
   xcell.exe --watch
   \`\`\`
   Особенности режима наблюдения:
   - Регенерация только изменённых файлов
   - Значительное повышение эффективности разработки
   - Поддержка предварительного просмотра в реальном времени

2. **Разумная конфигурация наблюдения**
   Настройте разумные шаблоны include/exclude для уменьшения количества наблюдаемых файлов.

### Оптимизация памяти

1. **Загрузка только необходимых таблиц**
   \`\`\`gdscript
   # Загрузка только определённых таблиц
   manager.buff_table.load()
   manager.item_table.load()
   \`\`\`

2. **Своевременная выгрузка**
   \`\`\`gdscript
   # Выгрузка ненужных таблиц
   manager.buff_table.unload()
   \`\`\`

## Частые проблемы

### Ошибки компиляции сгенерированного кода

- Проверьте правильность пути к проекту Godot
- Проверьте соответствие пространства имён структуре проекта
- Убедитесь, что все зависимости правильно установлены

### Сбой загрузки данных

- Проверьте, сгенерированы ли JSON файлы
- Проверьте правильность путей к файлам
- Убедитесь, что структура таблицы соответствует типам данных

## Лучшие практики

1. **Используйте формат мета-таблиц**: Для сложных конфигурационных таблиц рекомендуется формат мета-таблиц, поддерживающий более богатые определения метаданных
2. **Разумно используйте правила слияния**: Для больших проектов используйте правила слияния для управления сложными таблицами
3. **Оптимизируйте структуры данных**: Выбирайте подходящие типы данных и структуры в зависимости от реальных вариантов использования
4. **Регулярная очистка**: Регулярно очищайте ненужные конфигурационные таблицы и данные для поддержания порядка в проекте

## Пример проекта

XCell предоставляет пример проекта Godot, демонстрирующий использование XCell в реальных проектах:

- Базовое использование конфигурационных таблиц
- Сложные структуры данных
- Поддержка многоязычности
- Интеграция горячих обновлений

Через пример проекта вы можете быстро освоить лучшие практики XCell в Godot.
`);

console.log('\\nAll files created successfully!');
