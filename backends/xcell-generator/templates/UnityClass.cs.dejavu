// Unity generated file

using System;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

namespace {{ namespace }}
{
    /// <summary>
    /// {{ class_name }}表数据结构
    /// </summary>
    public class {{ class_name }}
    {
{% for item in items %}
        /// <summary>
        /// {{ item.field }}
        /// </summary>
        public {{ item.type }} {{ item.field }} { get; set; }
{% endfor %}
    }

    /// <summary>
    /// {{ class_name }}表加载器
    /// </summary>
    public static class {{ table_name }}
    {
        private static Dictionary<string, {{ class_name }}> items = new Dictionary<string, {{ class_name }}>();

        /// <summary>
        /// 加载{{ class_name }}表数据
        /// </summary>
        public static void Load()
        {
            // 从二进制文件或JSON文件加载数据
            // 暂时使用示例数据
            LoadSampleData();
            
            // 存储到 DataTableManager
            DataTableManager.Instance.SetTable("{{ class_name }}", items);
        }

        /// <summary>
        /// 加载示例数据
        /// </summary>
        private static void LoadSampleData()
        {
            items.Clear();
            
            // 示例数据
            // 实际项目中应该从文件加载
        }

        /// <summary>
        /// 根据ID获取{{ class_name }}
        /// </summary>
        /// <param name="id">ID</param>
        /// <returns>实例</returns>
        public static {{ class_name }} Get{{ class_name }}ById(string id)
        {
            if (items.TryGetValue(id, out var item))
            {
                return item;
            }
            return null;
        }

        /// <summary>
        /// 获取所有{{ class_name }}
        /// </summary>
        /// <returns>列表</returns>
        public static List<{{ class_name }}> GetAll{{ class_name }}()
        {
            return new List<{{ class_name }}>(items.Values);
        }
    }
}