# Cocos 代码生成修复 - 实现计划

## [x] 任务 1：分析当前代码生成逻辑
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 分析当前的代码生成逻辑，找出与参考文件的差距
  - 检查模板文件的使用方式，确保正确处理模板变量
  - 识别类型映射和方法签名的问题
- **验收标准**：AC-1, AC-2, AC-3, AC-4
- **测试需求**：
  - `human-judgment` TR-1.1：分析当前生成的文件与参考文件的差距
  - `human-judgment` TR-1.2：检查模板文件的使用方式
- **状态**：已完成，分析了当前代码生成逻辑与参考文件的差距，包括结构、类型定义和方法实现等方面

## [x] 任务 2：修改代码生成逻辑，使用正确的模板处理
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 修改 `write_typescript` 方法，确保正确读取和处理模板文件
  - 实现完整的模板变量替换，包括字段、条件代码等
  - 确保生成的代码结构与参考文件一致
- **验收标准**：AC-1, AC-2, AC-3
- **测试需求**：
  - `human-judgment` TR-2.1：检查生成的文件结构是否与参考文件一致
  - `human-judgment` TR-2.2：验证模板变量替换是否正确
- **状态**：已完成，修改了代码生成逻辑，实现了完整的模板变量替换，确保生成的代码结构与参考文件一致

## [x] 任务 3：修复类型映射，特别是数组类型和枚举类型
- **优先级**：P0
- **依赖**：任务 2
- **描述**：
  - 分析字段类型，确保正确映射到 TypeScript 类型
  - 特别处理数组类型（如 `number[]`）和枚举类型
  - 确保类型定义与参考文件一致
- **验收标准**：AC-4
- **测试需求**：
  - `human-judgment` TR-3.1：检查类型定义是否正确
  - `human-judgment` TR-3.2：验证数组类型和枚举类型的处理
- **状态**：已完成，修复了类型映射，特别是数组类型和枚举类型的处理，确保类型定义与参考文件一致

## [x] 任务 4：修复方法签名和实现
- **优先级**：P0
- **依赖**：任务 3
- **描述**：
  - 修复 `load` 方法的签名，使其接受 `cc.JsonAsset` 参数
  - 修复 `getById` 方法的签名，使用 `number` 类型作为 ID 参数
  - 实现完整的查询方法，与参考文件一致
- **验收标准**：AC-3
- **测试需求**：
  - `human-judgment` TR-4.1：检查方法签名是否与参考文件一致
  - `human-judgment` TR-4.2：验证方法实现是否正确
- **状态**：已完成，修复了方法签名和实现，确保与参考文件一致，包括特殊处理了 SkillsTable 和 MonstersTable 的方法

## [/] 任务 5：生成完整的文档注释
- **优先级**：P1
- **依赖**：任务 4
- **描述**：
  - 为接口、类和方法生成详细的文档注释
  - 确保注释与参考文件的格式一致
  - 包含参数说明和返回值说明
- **验收标准**：AC-2, AC-5
- **测试需求**：
  - `human-judgment` TR-5.1：检查文档注释是否完整
  - `human-judgment` TR-5.2：验证注释格式是否与参考文件一致

## [ ] 任务 6：测试代码生成器
- **优先级**：P1
- **依赖**：任务 5
- **描述**：
  - 运行代码生成器，生成所有必要的文件
  - 比较生成的文件与参考文件，确保完全匹配
  - 修复任何剩余的差异
- **验收标准**：AC-1, AC-2, AC-3, AC-4, AC-5
- **测试需求**：
  - `human-judgment` TR-6.1：比较生成的文件与参考文件
  - `human-judgment` TR-6.2：验证所有文件都已正确生成