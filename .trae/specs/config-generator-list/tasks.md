# XCell 配置格式升级 - 实现计划

## [ ] 任务 1：设计新的配置结构体
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 设计新的配置结构体，支持 `[[generator]]` 列表语法
  - 为每个生成器实例添加 `type` 字段，用于指定生成器类型
  - 确保新的配置结构体与旧格式兼容
- **验收标准**：AC-1, AC-2
- **测试需求**：
  - `programmatic` TR-1.1：验证新配置结构体能够正确解析包含多个生成器实例的配置文件
  - `programmatic` TR-1.2：验证新配置结构体能够正确解析旧格式的配置文件

## [ ] 任务 2：更新配置解析代码
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 更新 `ProjectConfig` 结构体的解析逻辑
  - 添加对 `[[generator]]` 列表的解析支持
  - 实现向后兼容逻辑，确保旧格式配置文件仍然可以正常解析
- **验收标准**：AC-1, AC-2, AC-3
- **测试需求**：
  - `programmatic` TR-2.1：验证解析代码能够正确处理新格式的配置文件
  - `programmatic` TR-2.2：验证解析代码能够正确处理旧格式的配置文件
  - `programmatic` TR-2.3：验证解析代码能够提供清晰的错误信息当配置格式错误时

## [ ] 任务 3：更新示例配置文件
- **优先级**：P1
- **依赖**：任务 1, 任务 2
- **描述**：
  - 更新 `e:\灵之镜有限公司\XCell\examples\rpg-untyped\ProjectSettings.toml` 为新的配置格式
  - 更新 `e:\灵之镜有限公司\XCell\examples\rpg-typed\ProjectSettings.toml` 为新的配置格式
  - 确保示例配置文件包含多个同一类型的生成器实例，以展示新功能
- **验收标准**：AC-1
- **测试需求**：
  - `programmatic` TR-3.1：验证更新后的示例配置文件能够被正确解析
  - `human-judgment` TR-3.2：验证示例配置文件的格式清晰、易理解

## [ ] 任务 4：测试配置解析功能
- **优先级**：P0
- **依赖**：任务 2, 任务 3
- **描述**：
  - 编写测试用例，验证新配置格式的解析功能
  - 测试向后兼容性，确保旧格式配置文件仍然可以正常工作
  - 测试错误处理，确保配置格式错误时能够提供清晰的错误信息
- **验收标准**：AC-1, AC-2, AC-3
- **测试需求**：
  - `programmatic` TR-4.1：验证新格式配置文件的解析
  - `programmatic` TR-4.2：验证旧格式配置文件的解析
  - `programmatic` TR-4.3：验证配置格式错误时的错误处理

## [ ] 任务 5：更新文档和注释
- **优先级**：P2
- **依赖**：任务 1, 任务 2
- **描述**：
  - 更新配置相关的文档和注释
  - 提供新配置格式的使用示例
  - 说明如何使用新格式定义多个同一类型的生成器实例
- **验收标准**：AC-1
- **测试需求**：
  - `human-judgment` TR-5.1：验证文档和注释的清晰度和完整性
  - `human-judgment` TR-5.2：验证使用示例的准确性和实用性