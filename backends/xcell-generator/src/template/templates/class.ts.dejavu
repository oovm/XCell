// <% table_name %>.ts
// Generated by XCell <% compiler_version %>

export class <% class_name %> {
<% loop field in class_fields %>
    public <% field.name %>: <% field.typing %>;
<% end loop %>

    constructor(data: any) {
<% loop field in class_fields %>
        this.<% field.name %> = data.<% field.name %>;
<% end loop %>
    }
}

export class <% table_name %> {
    private static _data: Map<<% id_type %>, <% class_name %>> = new Map();

    public static init(data: any[]) {
        this._data.clear();
        data.forEach(item => {
            this._data.set(item.<% key_name %>, new <% class_name %>(item));
        });
    }

    public static get(id: <% id_type %>): <% class_name %> | undefined {
        return this._data.get(id);
    }

    public static getAll(): <% class_name %>[] {
        return Array.from(this._data.values());
    }
}