# 類型系統（Type System）

XCell 支援多种資料類型，标准類型采用 Rust 约定，同时相容 C 约定和 C++ 约定。類型不協分大小農。

## 基本類型

### 整数類型

| 标准農法  | Unity 约定             | Unreal 约定  | 描述       | 范壙                                         |
| ----- | -------------------- | ---------- | -------- | ------------------------------------------ |
| `i8`  | `char`               | `int8_t`   | 8位元有符後整数  | -128 到 127                                 |
| `u8`  | `unsigned char`      | `uint8_t`  | 8位元无符後整数  | 0 到 255                                    |
| `i16` | `short`              | `int16_t`  | 16位元有符後整数 | -32768 到 32767                             |
| `u16` | `unsigned short`     | `uint16_t` | 16位元无符後整数 | 0 到 65535                                  |
| `i32` | `int`                | `int32_t`  | 32位元有符後整数 | -2147483648 到 2147483647                   |
| `u32` | `unsigned int`       | `uint32_t` | 32位元无符後整数 | 0 到 4294967295                             |
| `i64` | `long long`          | `int64_t`  | 64位元有符後整数 | -9223372036854775808 到 9223372036854775807 |
| `u64` | `unsigned long long` | `uint64_t` | 64位元无符後整数 | 0 到 18446744073709551615                   |

<br />

### 浮点数類型

| 标准農法  | C 约定     | C++ 约定   | 描述     | 精度  |
| ----- | -------- | -------- | ------ | --- |
| `f32` | `float`  | `float`  | 32位元浮点数 | 盧精度 |
| `f64` | `double` | `double` | 64位元浮点数 | 疊精度 |

### 布屬類型

| 标准農法   | C 约定   | C++ 约定 | 描述  | 值                |
| ------ | ------ | ------ | --- | ---------------- |
| `bool` | `bool` | `bool` | 布屬值 | `true` 或 `false` |

### 字元類型

| 标准農法    | 描述          |
| ------- | ----------- |
| `utf8`  | UTF-8 字元類型  |
| `utf16` | UTF-16 字元類型 |

### 字元串類型

| 标准農法     | 描述                 |
| -------- | ------------------ |
| `string` | 字元串類型              |
| `str`    | 字元串類型（與 string 等效） |

## 奧合類型

### 陣列與向量類型

**不同類型的约定**：

| 标准農法        | C# 约定            | 描述         |
| ----------- | ---------------- | ---------- |
| `[T]`       | `T[]`            | 勁态陣列       |
| `[T; N]`    | `T[N]`           | 静态陣列（固定大小） |
| `Vec<T>`    | `List<T>`        | 向量/串列類型    |
| `vec2<f32>` | `Vector2<float>` | 2D向量       |
| `vec3<f32>` | `Vector3<float>` | 3D向量       |
| `vec4<f32>` | `Vector4<float>` | 4D向量       |

**说明**：

- 勁态陣列 `[T]` 和向量 `Vec<T>` 用於儲存可變数量的元素
- 静态陣列 `[T; N]` 用於儲存固定数量的元素，长度在定義时確定
- 特殊向量類型（vec2/vec3/vec4）在圖形编程中常用，Unity 等引擎會有特定的效能最佳化

**範例**：

- `[i32; 5]` - 套件含5個i32元素的静态陣列
- `[string]` - 勁态大小的字元串陣列
- `Vec<i32>` - 整数向量
- `vec3<f32>` - 3D浮点向量

### 字典類型

**格式**：`HashMap<K, V>` 或 `dict<K, V>`

**範例**：

- `HashMap<string, i32>` - 字元串到整数的對應
- `dict<i32, string>` - 整数到字元串的對應

### 元組類型

**格式**：`(T1, T2, ...)`

**範例**：

- `(i32, string)` - 套件含整数和字元串的元組
- `(f32, f32, bool)` - 套件含兩個浮点数和一個布屬值的元組

## 特殊類型

### 顏色類型

| 類型      | 描述               | 格式                  |
| ------- | ---------------- | ------------------- |
| `color` | 顏色類型             | 十六進位元顏色值，如 `#FF0000` |
| `Color` | 顏色類型（與 color 等效） | 十六進位元顏色值，如 `#FF0000` |

### 时间類型

| 類型         | 描述     | 格式                                  |
| ---------- | ------ | ----------------------------------- |
| `datetime` | 日期时间類型 | ISO 8601 格式，如 `2023-12-25T10:30:00` |
| `time`     | 时间類型   | 如 `10:30:00`                        |
| `date`     | 日期類型   | 如 `2023-12-25`                      |

<br />

## 独一類型與主鍵

**独一類型格式**：

- `@T` - 資料表示该欄位元的值在資料表中必须唯一，即資料表中不能有兩行資料在该欄位元上有相同的值
- `@@T` - 資料表示该欄位元的值在資料表中必须唯一且作為主鍵

**主鍵说明**：

- 主鍵是資料表中用於唯一标识每一行資料的欄位元
- 如果資料表中没有使用 `@@T` 显式標記主鍵，那麼資料表的第一列會自勁被视為主鍵
- 主鍵值必须唯一且非空，用於在資料表之间建立茲联茲系

**範例**：

- `@@i32` - 整数類型的主鍵欄位元
- `@string` - 字元串類型的唯一欄位元（非主鍵）

## 自訂類型

### 列舉類型

通过 Enumerate 資料表格定義的列舉類型可以在其他資料表格中使用。

**範例**：

- `QualityType` - 參照列舉資料表中定義的品质類型

### 結構體類型

通过 Class 資料表格定義的結構體類型可以在其他資料表格中使用。

**範例**：

- `Position` - 參照 Class 資料表格中定義的位元置結構體

## 類型转换

XCell 支援自勁類型转换，例如：

- 整数可以自勁转换為浮点数
- 浮点数在需要时可以转换為整数（會截断小数部分）
- 數位可以转换為字元串
- 字元串可以在适当情淨下转换為數位

## 類型驗證

XCell 在剖析資料表格时會进行類型驗證，确保資料符合指定的類型要求。如果類型不匹配，會在編譯时生成錯誤。
